# uniontradingacademy.com

Website for **Union Trading Academy** — Order Flow | Volume Profile | Trading
Profesional (Spanish-language, es-MX). Domain and web hosting live in the
owner's IONOS Mexico (ionos.mx) account.

## Status (2026-08-04)

- Front page designed (more or less) in **IONOS MyWebsite Now** (site
  builder) but **not yet published** — the live domain still serves the IONOS
  "Construction" placeholder. See
  [`reference/`](reference/) for a screenshot of the builder design.
- **No live changes without the owner's permission.**
- Open decisions, in order:
  1. **Hosting/deploy approach** — stay in the MyWebsite Now builder vs.
     rebuild as code in this branch deployed to IONOS webspace (the latter is
     what makes "revert to any prior date" real, via git history).
  2. **Design direction** — Claude runs the design; owner signs off.

Layout will follow the house convention once building starts (compare
`www.buitendyk.ca/` on the `website` branch): site files under `sites/`,
deploy tooling under `deploy/`.
