# General Classifier environment. Nothing secret lives here; the app has no
# accounts, keys, or AI/API access. PORT must match the nginx location on the
# website branch (/classifier/ -> 127.0.0.1:8093).
PORT=8093
