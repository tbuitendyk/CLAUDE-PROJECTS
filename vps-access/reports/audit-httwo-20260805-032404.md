# Post-run audit — httwo-20260805-032404-ht2-36mo-ltcusdt (engine 1.41.0)

First real-candidate run of the exam-cleared HT v2 instrument. 36-month
half-life vs reference on LTCUSDT daily-4d argmax q5 (the owner's frozen
selection from the 0733 board). Fired by the owner from the page.

## 1. What was this run supposed to answer?
Declared before the run (DESIGN-HT2.md + the run's stamped description):
does fading old training data with a 36mo half-life beat untouched training
on this row, judged by the sign-flip test on per-fold dollar differences at
the pre-declared p <= 0.05? Hypothesis origin: the v1 design read (curtain
opened 2026-08-04) — contaminated for selection, cited as origin only.

## 2. Did it answer THAT question?
Yes. Paired sum +$226.40 over 20 folds, 11 positive, p = 0.0700,
concentration 23%, carriedByOneFold false. Recorded verdict: NO EFFECT
SHOWN. The declared rule is the whole point of the pre-commitment; 0.07 is
not talked into a pass.

## 3. Instrument-hunting notes
- Fold bookkeeping clean: 20/20 completed, 0 dropped, 0 silentBothArms.
- Ruler context from the exams (same engine, same fold count): known-real
  planted effect read +$262 / 13 pos / p 0.021; known-nothing read -$48 /
  9 pos / p 0.82. This run (+$226 / 11 pos / p 0.07) sits near the
  known-real reading — the honest statement is "at 20 folds the instrument
  cannot resolve an effect of this size from noise", NOT "there is nothing
  here" and NOT "there is something here".
- The p <= 0.05 line is GUESSED (convention), declared before the run. A
  guessed threshold missed by 0.02 is a prompt to look harder via
  REPLICATION, never a licence to re-read this run as a pass.

## 4. Candidate status
NOT retired (no candidate is retired on one measurement) and NOT promoted.
Weak prior (contaminated origin) + near-line reading. The clean next test
is replication across assets: the same declared 36mo paired test on a
different promoted survivor row. Pre-declared reading for that run, stated
now: same p <= 0.05; if the sibling also shows nothing, the 36mo lead is
retired on failed replication plus contaminated origin; if it passes, the
LTC 0.07 reading is re-counted as a near-miss consistent with a small real
effect and a higher-resolution (more-folds) run is designed FRESH.
Re-running LTC itself with more folds after seeing 0.07 is optional
stopping and is off the table.

## 5. Assumed and not verified
- ASSUMED the 20-fold sensitivity measured on the planted pair transfers
  roughly to real pairs (different vol, different signal density).

## QC register
Nothing new caught by this run. Two pending UI items from tonight's
session (stale "hours" cost note on the v1 launcher; legacy v1 button more
prominent than the live v2 one — caused an owner mis-click) go with the
next deploy.

## Amendment (2026-08-05, before any further run) — LTC is a PEEKED pair
The owner is considering 12mo on LTCUSDT. Declared before it fires:
- Any further half-life tried on LTCUSDT is a member of one family with
  the 36mo try. Family-corrected pass line: p <= 0.05 / (tries taken).
  At two tries that is p <= 0.025. An uncorrected 0.05 reading is
  recorded as a LEAD only.
- No LTC-only reading, at any half-life, confirms the age-weighting
  hypothesis from here on: LTC is peeked. Confirmation requires a pass
  on a different promoted row at the full 0.05 line.
- Recommended instead (stated for the record): replicate 36mo on a
  different promoted 0733 row — unpeeked data, full-strength line,
  same cost.
