# Post-run audit — job walkforward-20260801-215650-wfnull2-s101

Engine 1.31.0. 136/136 setups scored, 0 failures, 223.4 min. FIRST
rebuilt null run (register 66 deal construction: each member's real mix
of votes dealt onto random days by a seeded shuffle — zero market
information by construction, owner-designed). Its launcher declared it
carries NO verdict alone; it exists, with seeds 102–104, to feed the
four-arm paired read on the clean construction.

---

## 1. What was this run supposed to answer?

Nothing by itself (declared). It is one of four draws of the rebuilt
null the pre-declared line is measured against.

## 2. Does the output answer THAT question, or a neighbouring easier one?

It contributes as declared. Counts this draw: R1 11, R1+R2 5, S1 57
(real board: 22, 7, 63). Read through the fixed reader targeting
-wfnull2- docs only; old rotation-built runs are superseded and were
not touched.

## 3. What does the metric COUNT that it should not?

As registered (QC 64); nothing new. Worth recording: this rebuilt
draw's R1 count (11) sits ABOVE every rotation draw's (5–13 range
across the four superseded seeds) — consistent with the register 66
diagnosis that rotation leaked market knowledge in neither a uniform
nor a conservative direction. No conclusion from one draw; noted so
the four-arm read is checked against it.

## 4. What does the metric OMIT that it should include?

Magnitudes — handled by the paired read once all four rebuilt seeds
exist.

## 5. Are the two compared arms the same population?

Yes. Same universe, same permute flags, same minTradesSlice, same
feePerLeg as WF1; the compare endpoint's population fingerprint
enforces this and excludes '-smoke' and pre-1.31 docs.

## 6. Is any part of the reported number achievable with NO skill?

This run IS that measurement: 5 of 136 pass R1+R2 with dealt votes.

## 7. Would this number look the same on pure noise?

It is pure noise by construction. Spread across rebuilt seeds unknown
until 102–104 land — that spread is what sets the line.

## 8. What did I assume and not verify?

Worker-scheduling determinism — still open, still queued. The planted
check PASSED on this exact engine (1.31.0, deal construction) before
this run fired, so the instrument was calibrated immediately prior.

## 9. Is the previously planned next step STILL correct?

Yes: fire seed 102 via classifier-wfnull2-next.sh (identical body,
declared), then 103, 104, audits between; then the four-arm paired
read through the pre-declared line — real paired-supported count must
exceed max(null-vs-rest counts) + (max − min). No changes.

## 10. New QC-REGISTER entries

None; nothing was caught, and the declared plan proceeds unmodified.
