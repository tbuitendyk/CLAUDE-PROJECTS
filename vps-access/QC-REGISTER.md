# QC register

Owner's rule, 2026-07-29: **every time a faulty or incomplete assumption is
caught, it becomes a permanent non-negotiable QC item.** Not a lesson to
remember — an item on this list, with a named place it is enforced.

The test of this document is simple: if an item is marked AUTOMATED, there is
a test or a tool that fails when the fault is reintroduced, and that failure
has been *observed*, not assumed. An item nobody has watched fail is not a
check. Items that cannot be automated are marked MANUAL and say who does what.

Why this exists in this form: every serious defect in this project has been an
**instrumentation** defect, not a maths defect. A broken sweep throws an error.
A broken measurement returns a number with the right units and a plausible
magnitude, and gets built on. Six of the entries below were caught only because
a number looked *encouraging*.

| # | The assumption | What was actually true | The standing check | Enforced in | Status |
|---|---|---|---|---|---|
| 86 | A software "position" maps to its own balance on the exchange | Not on isolated margin, which pools ALL positions of a pair into ONE wallet and ONE loan. The executor sized every exit from the wallet-wide balance (free_base for longs, borrowed_base for shorts), so with F1's up-to-6 concurrent positions the FIRST exit would sell/repay EVERY open position at once — booking 6× size against one position's cost basis, orphaning the rest, corrupting realized P&L and the loss-kill, and tripping reconcile→halt. Missed entirely in my own code and caught only by an adversarial money-math review; the dust and single-position tests never exposed it because they only ever had one position open | Every close is sized from the INDIVIDUAL position (p['qty']), with the wallet aggregate used only as a cap that can never enlarge the order: long sells floor_step(min(p_qty, free_base)); short buys ceil_step(p_qty·(1+buffer)) capped at the pooled debt. Held qty is stored UNFLOORED so reconcile stays calibrated across many positions. Any test with ≥2 concurrent positions must show the first exit trading only its own size | executor/mx_executor.py do_run exit loop; tests test_two_longs_first_exit_sells_only_its_own and test_two_shorts_first_close_repays_only_its_own (watched failing with wallet-wide sizing, then passing) | AUTOMATED for the sizing — watched failing 2026-08-11. MANUAL note: a full net-position redesign would be more robust than per-fiction-position orders; revisit if concurrency grows |
| 85 | If a buy of quantity Q fills, we can later sell Q | Not when the taker fee is charged in the asset transacted. A BUY takes its fee in the BASE asset (LTC), so a $10 buy leaves slightly LESS than Q sellable and selling Q fails 'insufficient balance' (-2010); a short CLOSE has the mirror problem — buying exactly the borrowed Q leaves a residual borrow that accrues interest and hides under the reconcile tolerance. Seen LIVE in the first dust trade (2026-08-11): bought 0.221 LTC, sell of 0.221 rejected, position left open and flattened by hand | Orders are sized from what is actually held/owed after fees: sells floor DOWN to the lot step (never round up into insufficient balance); short closes round UP with a fee buffer so AUTO_REPAY fully clears the loan; fees are valued in quote terms whatever asset they are charged in. A dust trade in EACH direction proves the round trip before the engine is armed | executor/mx_executor.py fills_summary/floor_step/ceil_step + entry/exit/dust; tests test_buy_fee_in_base_shrinks_the_sell_qty, test_long_exit_sells_free_base_after_fee_not_nominal, test_short_round_trip_fully_repays_despite_base_fee (all watched failing then passing); live long dust round-trip cost $0.0222, ~10bps/leg | AUTOMATED (tests) + live long dust confirmed; live short dust pending owner go |
| 84 | A health probe returning "everything is refused" means something has broken | Not when the thing being probed reboots on a schedule. On 2026-08-10 a heartbeat read `IMAP login FAILED: No route to host`, `mailvm-liveness.sh` showed the guest pinging with 22/25/587/465/143/993/5432 all refused, and the session called it an outage and wrote a diagnostic for it. The restart log then showed the cause: the daily 09:00 UTC timer had fired 4min 45s earlier — reboot requested 09:00:14, guest down 09:00:29, new boot id 09:03:28 — and the mailbox had simply been read mid-boot. The pre-restart snapshot was clean (seven services active, empty queue, zero OOM kills), and by the next check 22/143/993 were open and mail read normally. Nothing was broken; the probe was aimed into a known blind window | Before any probe failure is called a fault, check whether a scheduled restart is in flight: elapsed time since the last `restart requested` line is PART OF THE READING, not context to look up afterwards. A monitor watching a periodically-restarting service treats one failed poll as "unknown", never "down"; only a failure persisting past the restart's own recovery budget is evidence | scripts/mailvm-outage-diag.sh opens with the restart-window check and prints an EXPECTED banner when a restart is under 20 min old; the mail watcher requires two consecutive failed polls before emitting WATCHER-BROKEN | AUTOMATED for the diagnostic's ordering — watched printing the EXPECTED banner while inside the window. MANUAL for the judgement that a persistent failure is real |
| 83 | A backtest number measures the setup | It measures the setup AND the training extent it happened to be given, and for some setups the second dominates. Holding the scored periods fixed and varying only how much history trains the members, the XLM/directional setup that cleared the money screen moved from -$85.77 to +$101.27 with a call rate spanning a factor of 63. Its selected figure of $40.96 was one draw from a range straddling zero; another extent would have flipped its sign and it would never have become a candidate. LTC/argmax over the same sweep held a positive sign at a x1.6 spread | Training-extent stability is a standing gate before any live-money conversation: sweep training extent with the scored periods held fixed, and require net money to keep its SIGN and the call rate to stay within a factor of two (both thresholds GUESSED, declared in advance). Failing it bars a setup from money, not from research | scripts/node/trainextent.js + classifier-trainextent-fire.sh/-read.sh; reports/audit-trainextent-2026-08-07.md | MANUAL — the sweep must be run and read per candidate; no automated gate exists yet, and it is an open gap that nothing blocks a live conversation mechanically |
| 82 | An instrument check I declare before a run is automatically the right check | Not if its tolerance is invented rather than derived from the mechanism it tests. Run E's launcher demanded that null trade counts BRACKET the real arm's, and declared the verdict void otherwise. It came in at real 310 vs nulls 307-309 — outside by ONE trade — and read literally that voided a valid run. The shared-permutation fix guarantees an identical multiset of quorum CALLS (asserted directly by test), not an identical count of EXECUTED trades: a 161h hold overlaps following periods and edge calls cannot complete, so permuting drifts the executed count by a trade or two. The threshold tested something the fix never promised, and it was written without the DERIVED/GUESSED label the register already requires | Every declared threshold states DERIVED or GUESSED and names the mechanism it is derived FROM; a check on a quantity the fix only approximately controls carries a tolerance plus a materiality test (residual x per-unit value, as a share of the measured margin), not exact equality | reports/audit-...-confirm-e.md section 1 (re-specified check: within 2% of the null range edge AND residual worth under 5% of the margin; run E passes at 0.3% and 0.1%) | MANUAL — a judgement rule at launcher-writing time; no linter can tell a derived threshold from an invented one |
| 81 | Dealing each member's votes independently removes only TIMING information | It removes their AGREEMENT too, and that changes how often the arm trades. The committee takes a position only when its members are not tied, so independent deals break ties apart and the null trades less often than the real arm for reasons unrelated to skill. Measured on run D's own candidate: the real arm traded 310 of 364 held-back periods while ALL NINETEEN draws traded 255-296 — the arm sat entirely outside the range of the control judging it, and its pass is withdrawn. Raised as an untested objection by the adversarial audit, then confirmed from participation data already sitting in the run's stored output | Null arms are dealt with ONE permutation shared by every member, so the multiset of quorum calls (up/down/aside counts at every agreement level) is preserved exactly and only market alignment is destroyed | tests/test-gatepipe.js nullArmsPreserveCommitteeAgreementAndTradeCount (drives the real worker on the fabricated pair, checks every agreement level) | AUTOMATED for the board path — watched failing 2026-08-07 with per-member deals restored, then passing. OPEN GAP: lib/walkforward.js still deals per member, so no walk-forward null is evidence until fixed the same way |
| 80 | A null arm that keeps the real vote MIX is a fair information-removal control | Not if the mix is pooled across windows. The board path (lib/bracketwork.js) dealt each member's votes across the CONCATENATED test+hold span and sliced afterwards, so every null arm's hold window carried the pooled mix of both windows while the real arm kept the hold window's own mix. In a one-directional hold window (always-long -$657.82, always-short +$478.32) that gap pays by itself, so the real arm was credited for its overall directional lean, not only its timing — and no number of extra draws could reveal it, because every draw shared the defect. Two symptoms in the run C data agree: sibling arms of the same window/coins sit at z=+3.30 and z=-4.04 of their own nulls, which an information-removal null should be unable to produce. lib/walkforward.js has always dealt per slice; only the board path was wrong. Caught by an adversarial audit of the confirm chain, not by any test | Null arms are dealt WITHIN each scored slice, never across a concatenated span; any new scoring path that deals votes asserts per-slice multiset equality against the real arm | tests/test-gatepipe.js nullArmsAreDealtWithinTheirOwnSlice (drives the real worker on the fabricated pair) | AUTOMATED — watched failing 2026-08-07 with the fault reintroduced (null hold mix differed from real hold mix), then passing |
| 1 | Settings sent to the API reach the sweep | `trailing` and `holdout` were dropped; every "trailing" run had trailing off | Every param the orchestrator reads must be forwarded by the endpoint | `everyBracketParamSurvivesTheApi` (test-bracket.js) | AUTOMATED — verified by deleting the forwarding |
| 2 | A run used the settings its launcher asked for | Only the launcher was checked, never the run | Read the construction back off the run document and compare to the census rows | `classifier-edge-read.sh` (`null construction:` line) | AUTOMATED |
| 3 | "edge > 0 about half the time" is the null | Measured null is ~54%, and it moves | Never assume a null; measure it, and print the measured value beside the result | `classifier-edge-read.sh` | AUTOMATED |
| 4 | Rotations are interchangeable draws of one statistic | Series-scope rotation moved the majority baseline 0.265→0.419, so draws were scored against different yardsticks | Rotate inside each window; hold the baseline fixed; record it per row | `labelShiftScope: 'window'`, `windowRotationHoldsTheBaseline...` (test-bracket.js) | AUTOMATED — verified by mutation |
| 5 | Draws that disagree can still be averaged | −2303's draws spanned 76.5 points and were nearly reported as a market result | Gate at 15 points; refuse to print the comparison when it fails | `classifier-edge-read.sh` GATE FAILED | AUTOMATED — observed failing on −2303 |
| 6 | Every unit in the census is participating | 15% of rotated units never trade, and are scored as wins on flat calls alone (2.4% in the real run) | Print participation, a mute column per draw, and active-only beside all-units | `classifier-edge-read.sh`, `classifier-census-diag.sh` | AUTOMATED |
| 7 | The binomial p-value measures significance | Scrambled data produced pooled p = 0.0047; units are correlated, so the test is invalid | **Never quote a binomial p as evidence.** The measured null is the only yardstick | this register + reader wording | MANUAL — see gap G1 |
| 8 | A screen read off the leaderboard is a census | The leaderboard is money-ranked and capped, so the read was P&L-selected | Census rows are recorded separately and never money-filtered | `doc.edgeCensus` (batch.js) | AUTOMATED |
| 9 | An unknown task kind is impossible | The pool's fallback picked by a two-way guess and would silently run the wrong function | Dispatch by name; unknown kinds are an error | `inlineDispatchRefusesUnknownKinds` (test-pool.js) | AUTOMATED — verified by mutation |
| 10 | The workers are niced because the code says so | Nothing could observe it; `ps` was ambiguous | Ask the workers and read the kernel's number from procfs | `/api/selftest/workers`, `classifier-worker-selftest.sh` | AUTOMATED — PASS observed on the box |
| 11 | `VBoxManage` VMState tells you if a guest is alive | Reported `poweroff` for a guest that was serving mail at that moment | Drop VMState; use the process plus a TCP reachability probe | `host-health.sh` | AUTOMATED |
| 12 | A service that fails to answer is a starved host | IMAP answered while SMTP timed out — it was one daemon, not the guest | Probe a second service on the same host before escalating | `mailvm-postfix-restart.sh` (escalates only if ssh fails) | AUTOMATED |
| 13 | A restart that reports "active" worked | Postfix reported active with nothing listening | Assert the ports, not the unit state | `mailvm-postfix-restart.sh` | AUTOMATED |
| 14 | A long remote command will finish inside the default timeout | A 2-minute cap killed a restart mid-flight and left mail down 3 minutes | Any guest command gets an explicit long timeout; restarts must be safe to re-run | `mailvm-postfix-restart.sh` | MANUAL — see gap G2 |
| 15 | Noticing a message is free | The poller consumed messages by reading them | Monitoring uses the read-only twin only | `claude-mail-recent.sh` vs `claude-mail-check.sh` | MANUAL — habit, see gap G3 |
| 16 | One watcher's marker is its own | Two watchers shared a marker; the first to finish suppressed the second's announcement | Marker is per-job | `watch.sh` | AUTOMATED |
| 17 | A job id passed to a script arrives | The deploy API forwards no arguments; older runs could not be read at all | Selection goes through a committed file | `reports/EDGE-JOB` | AUTOMATED |
| 18 | "The numbers are lost" | They were retrievable; the tooling just did not exist yet | Never report a gap in tooling as a fact about the world | this register | MANUAL — see gap G4 |
| 19 | A hypothesis flagged "cheap to check" will get checked | It did not, until the owner pushed | Anything flagged unverified is either checked in the same session or written down here as open | this register | MANUAL — see gap G5 |
| 21 | A pre-registered metric must never be changed, even once shown to be broken | Pre-registration exists to stop metric SHOPPING (keeping whichever measure flatters you). It does not require keeping a measure proven to count things that cannot express the quantity — that is stubbornness wearing rigour's clothes. Caught by the owner, 2026-07-29 | A metric may be corrected mid-stream **only** when: (a) the fault is argued from MECHANISM, not from the number improving; (b) the correction moves BOTH the real result and the null, not just the favourable one; (c) the old measure keeps being reported beside the new one. All three, stated at the time | this register | MANUAL |
| 20 | Acting on a hypothesis before gathering data is fine | A job was cancelled on a guess; the real cause was contention | Gather the cheap evidence first | `host-health.sh` exists so the question is cheap | MANUAL |

| 22 | A classification score is a trading score | `edge` grades flat calls, which place no trade. A setup with 1 committed call and 130 flat calls was graded almost entirely on the flats | Primary measure is pooled directional accuracy: of the periods where a direction was called, how often it was right | `classifier-census-diag.sh`, CYCLE6-READING-RULE | AUTOMATED (reported) / MANUAL (which measure governs) |
| 23 | Removing non-participants makes two arms comparable | It does not. Real run: 77.6% of setups made 21+ trades; scrambled: 59.8%. The arms still differ after the zero bucket is dropped | Check the whole activity distribution, not just zero; prefer per-decision pooling, which is robust to it | `classifier-census-diag.sh` call-count buckets | AUTOMATED |
| 24 | A unit-level headcount summarises the data | 170 yes/nos discard the 19,913 decisions underneath and inherit flat-call contamination. The headcount overstated the effect ~3x (5.8 pts vs 1.9 pts) | Report the per-decision metric beside any headcount | `classifier-census-diag.sh` | AUTOMATED |

| 25 | One exit code can serve two failures | `claude-mail-send.sh` returned 3 for both "message malformed" (never retry) and "transport failed" (always retry). Any automated caller would have to get one of them wrong | Malformed = 2/3, transport = 4. Distinct codes for opposite responses | `claude-mail-send.sh` | AUTOMATED |

| 26 | A gate that passes is a pass like any other | Cycle 6's spread gate passed at 2.44 against a 2.5 limit — 0.06 points from discarding the run | Report the MARGIN on every gate, not just the verdict. A near-miss is information about the instrument | audit template Q2 | MANUAL |
| 27 | The significance floor is the significance | With N draws the rank test cannot go below 1/(N+1). "p = 0.05" on 19 draws means "the best this design can say", not "a strong effect" | State the floor beside the result | `classifier-edge-read.sh` prints the floor | AUTOMATED |
| 28 | Metrics measuring related things will agree | They did not. Directional accuracy put cycle 6 above all 19 scrambles; the active-only headcount put it INSIDE, 2.2 points below the top draw | When metrics disagree, the disagreement IS the finding. Which one governs must be declared in advance, with the reason | CYCLE6-READING-RULE, audit Q9 | MANUAL |

| 29 | Some loop steps belong to the owner | **ALL SEVEN STEPS ARE THE SESSION'S.** The loop never hands off, at any point. I stopped after step 3 and wrote "awaiting your word", repeatedly — then, correcting it, still described the loop as "assigning steps 1-4 to me", implying a boundary that does not exist. The owner: "THEY ARE ALL YOUR STEPS." Waiting IS the failure: the owner sleeps, the account is paid for by the hour, and an idle session produces nothing | Never stop at a step boundary. The safeguard is the reading rule + stated expectation committed to the launcher BEFORE the numbers exist — not asking. Escalate only for a NEW kind of risk the loop does not cover: real money, destructive/irreversible actions, anything outward-facing. "Which experiment next" is step 1 and is never an escalation | CLAUDE.md loop preamble (unmissable, top of the section) + this register | MANUAL |

| 30 | A watcher can put mail and jobs on one timer | It cannot. Mail needs 300s (the owner's number, about responsiveness to him); a 5-hour job needs almost nothing until it is nearly due. Sharing one timer polled a 5-hour job 141 times, and every wake costs tokens as well as requests | **NON-NEGOTIABLE: never check a running job more often than it could plausibly need — never every 2 minutes.** Derive the job interval from time REMAINING (hourly while >1h out); keep mail on its own 300s loop | `watch.sh` (separate `JOB_EVERY` / mail `sleep 300`), CLAUDE.md loop step 6 | AUTOMATED |
| 31 | A hand-written timestamp constant is fine | I hand-wrote a job deadline as a unix epoch and it landed in 2025, making "time remaining" negative and silently forcing the tightest polling interval. It would have run forever, wrongly, without complaining | Never hand-write an epoch: derive it (`date -u -d ... +%s`) and print the derived values back to confirm | `watch.sh` due-epoch, computed not typed | MANUAL |

| 32 | An analogy that worked once transfers | "Pool per TRADE like we pooled per decision" was reasoning by resemblance. Each scramble makes ~42,000 trades, so dividing by it is near-pure rescaling — and a spread-to-magnitude RATIO is scale-invariant, so it could not possibly improve. Measured: 2.25 vs 2.29 | Check the arithmetic of the analogy before building on it | this register; audit template Q8 | MANUAL |
| 33 | A gate that suits one statistic suits another | A spread-to-magnitude ratio is meaningless for a statistic centred at ZERO by construction — dividing by a near-zero median makes any spread look enormous. Borrowed from the accuracy headcount, where the null sits near 50% and a wide spread really does mean a broken build | Gates are per-statistic and must be DERIVED. For zero-centred quantities use the rank test, which assumes nothing about spread | CYCLE8 rule replacement, `classifier-money-read.sh` | MANUAL |
| 34 | A census change applies to runs already made | Deployed the money census at 08:10, ran the null at 08:11, and the real arm was from 07:29 — so the null carried money and the real arm did not. A 5-hour run with nothing to compare against | After ANY census change, re-run BOTH arms on the identical build before comparing | this register | MANUAL |

| 35 | Two results from one run describe the same configuration | The money cell is chosen by search-window MONEY (35,678 trades); the accuracy rung by search-window EDGE (19,913 calls). Different selections — so the accuracy edge cannot be said to "convert" into the money result | State which selection produced which number; never let one validate the other | audit template Q5 | MANUAL |
| 36 | A headline dollar figure is achievable | $1,469.95 is a sum over 170 overlapping setups on 17 coins — ~10 simultaneous configurations per coin. Nobody could trade it. It is a statistical aggregate for comparison against the same aggregate on noise | Never quote a census total as profit; say what it is | audit template Q3 | MANUAL |
| 37 | A positive net result is robust | Fees consume 85.9% of this gross edge. Net $0.0412/trade sits on gross $0.2912 against assumed costs of $0.2500 — 16% headroom. A worse fee tier or ordinary slippage makes it negative, and none of that is visible in the net figure | Report gross, fees and break-even cost beside every net number | `classifier-money-verdict.sh` (to add), audit Q6 | MANUAL |

| 38 | `null` unambiguously means "none" | Against a value-based fallback it means "inherit the default". `labelShiftFrac != null ? labelShiftFrac : p.labelShiftFrac` turned the real arm's explicit null into the run-wide 0.5 | Resolve optional per-item overrides by KEY PRESENCE (`hasOwnProperty`), never by value | `theRealArmCannotBeSecretlyScrambled` (test-bracket.js) | AUTOMATED — watched failing |
| 39 | A run with zero failures ran correctly | Failure count counts crashes, not correctness. Cycle 10: 3400 units, 0 failures, 407 minutes, void — its real arm was a scramble | Never read "fails=0" as validity. Check an invariant of the OUTPUT: here, that no two arms are bit-identical | `classifier-money-precision.sh`; audit Q5 | MANUAL |
| 40 | A fix cannot introduce a fault | The r=0 real arm was added to make a MISSING real arm impossible (QC 34) and instead made a FAKE one inevitable, same file, same day | After any structural fix, verify the artefact it produces is what it claims. An exact tie between arms is the cheap tell | this register; audit Q8 | MANUAL |

| 41 | A gate failing is a verdict on the candidate | It is a verdict on the INSTRUMENT. Cycle 6's spread gate passed at 2.44 against a limit of 2.5 that I invented; at 2.57 I would have discarded the run and possibly the line of work on 0.07 of made-up margin. Which side it landed on was luck — the process was wrong either way. Owner: "you came within a shade of rejecting things which very well might prove good" | No candidate retired on one measurement — retirement needs replication across periods or a mechanism. Every threshold labelled DERIVED or GUESSED when written. A failing gate is examined before it is obeyed | CLAUDE.md "Gates judge the INSTRUMENT" | MANUAL |
| 42 | Narrowing the search is not the same as discarding | It is. All 10 cycle launchers ran `singles` only with `trailing: false` — doubles, triples and trailing stops silently absent from every run since cycle 1, never justified, never revisited. A quiet default retires a variant more effectively than an argued rejection, because nobody notices | Any variant switched off must be named in the launcher's rationale with a reason and a date to revisit. Audit the launchers for silent narrowing | this register; audit template Q9 | MANUAL |

| 43 | The param-forwarding guard covers all settings | It scanned for `params.X` reads, so a setting HARD-CODED from a constant was invisible — nothing reads it from params, so nothing looks missing. `feePerLeg` sat unreachable that way while being the one dimension cycle 9's result depends on | Guard now checks BOTH directions: anything the sweep consumes as `p.X` must be sourced from `params.X` inside startBracketLab's own body (scoped, because other job types read the same key for themselves) | `everyBracketParamSurvivesTheApi` (test-bracket.js) | AUTOMATED — watched failing |
| 44 | A test that passes is a test that works | Three iterations of the new guard passed while unable to catch the fault: first regex stopped at a comma, then a file-wide search matched another job type's use of the same key. Each version passed the suite and detected nothing | A new check is not trusted until the fault is REINTRODUCED and it fails. Passing proves nothing about a check | this register; the mutation step in every fix | MANUAL |

| 45 | A suspicion about a metric will favour the arm you raise it for | I checked whether outliers were unfairly sinking the real arm in cycle 11. They were sinking the SCRAMBLES harder — worst-5 was 63.3% of real's total against a scramble median of 89.8% | Check the direction of a suspected bias before reporting it; a suspicion is a hypothesis, not a finding | audit template Q3 | MANUAL |
| 46 | An approved next step survives its own premise | The fee sweep was approved before cycle 11 removed its purpose. Fees cannot explain a failure the scrambles also paid — every scramble paid the same fee and the real arm still came last | An approval is for a QUESTION, not for a job. When step 7 removes the question, say so and re-plan rather than executing the approved thing | loop step 7c; this register | MANUAL |
| 47 | Overlapping geometries give independent evidence | Cycle 11's four worst setups were one asset (XRPUSDT) in daily-1d/2d/3d/4d — re-cuts of the same days, so one failure counted four times, 44.9% of the total loss from three rows | Report per-asset concentration beside any total | `classifier-cycle11-diag.sh` | AUTOMATED (for this shape) |

| 48 | A pinned selector applies only to the investigation that pinned it | EDGE-JOB stayed pinned to L9 for a day; the first L13 read silently returned L9's numbers — which, being a reproduction check, LOOKED exactly right | Every reader prints the doc id it read (this is what caught it); reset pins as part of the read that used them | `classifier-*-read.sh` headers | AUTOMATED (id printed) / MANUAL (reset habit) |
| 49 | The committee's "interlaced" training regime adds diversity | It never interlaced. It is purge-only: the same contiguous window with ~10% of chunks trimmed at internal 49-day seams that protect nothing (no second group exists there). Six of twelve members are near-copies of the other six — same view, same algorithm, ~90% identical rows — so quorum counts read inflated ("8 of 12" may be ~5 independent opinions) and the search optimizes the quorum dial on that distortion. Owner (2026-07-30): likely diluting signal quality. Confirmed against his recovered design prompts: block-interlaced A/B group assignment (metalens.js) was the design; pipeline.js interlacedPurge carried the name without the mechanism | The inspect panel's pairwise matrix makes it visible (full-vs-interlaced twins are the hot >=80% cells). Regime's fate — drop or truly interlace — is an L14 consult decision; until then no result may cite "two training windows" as member diversity | `everyCommitteeSeatIsADistinctOpinion` (test-bracket.js) | AUTOMATED — watched failing (12 !== 6 with the regime reintroduced), removal shipped 2026-07-30 |
| 50 | Equal potential trade slots imply equal actual periods | They do not: missing-candle gaps fall unevenly across history, so the interlaced arm's era-sprinkled eval blocks carried fewer ACTUAL chunks (501/498) than the chronological arm's gapless recent windows (504). A gapless synthetic test could never catch this — only the end-to-end smoke on real data did | Both arms compute both layouts and trim their eval sets to the common per-set count, deterministically; every new mechanism gets an end-to-end smoke on REAL data before it is announced | `gappyDataStillYieldsIdenticalActualPeriods` (test-interlace.js, watched failing 126 !== 124); `classifier-layout-smoke.sh` (watched failing live, 2026-07-30) | AUTOMATED — watched failing |
| 51 | When a function's return shape changes, its consumers were all updated | They were not: trainMembers began returning member objects in L12 and nullRotationTask kept consuming them as call arrays — every quorum vote read undefined, every rotated world stood aside, and the survivor-null "distribution" was a committee-free world for weeks. Silent, because undefined counted as an abstention | quorumCall throws on any non-vote value, so a wrong shape crashes instead of abstaining | `quorumCallRefusesNonVoteShapes` (test-interlace.js) | AUTOMATED — watched failing |
| 52 | Purging the label window removes all train/eval contamination | It does not: an evaluation trade entered at entryOffset can stay open to the menu's longest timeout + the 3h gap-scan, far past the label window — and train chunks whose FEATURES sit in that path let the committee train on candles that price its own evaluation trades. One-sided (interlaced arm only), i.e. a systematic bias in the exact comparison phase 2 was built for | Purge reach = max(features, label end, entry + longest menu timeout + 3h), derived per job from its own tHours menu | `trainingNeverSeesAnEvalTradePath` (test-interlace.js) | AUTOMATED — watched failing |
| 53 | "No value" and "explicitly null" can share one representation (recurrence of QC 38) | Payload builders collapsed absent-stance to explicit null, so run-wide labelShiftFrac jobs never rotated and discovery-mode promotion reran scrambled slim rows unrotated. Same class as QC 38, caught in NEW code by the audit — the lesson transferred to the register but not to the fingers | All rotation stances flow through shiftStance(): key PRESENCE is the stance, everywhere | `rotationStanceRidesOnKeyPresence` (test-interlace.js) | AUTOMATED — watched failing |
| 54 | Buy-and-hold is a valid drift control under any window layout | Under interlacing it spans the TRAINING gaps between evaluation blocks — market moves the strategy was never allowed to trade. DOGE daily-3d L14: vsHold -$8,287 purely from buy-hold riding the 2021 mania across the gaps | Under scattered layouts, drift control = vs-always-long (sum of per-period holds) only; vsHold printed but labeled invalid for layout runs | classifier-cycle14-read.sh prints both; audit template Q3 | MANUAL |
| 55 | The two layout arms differ only in window placement | The interlaced arm pays purge at ~12 seams (up to 335 train chunks) vs the chronological arm's 2, so the comparison conflates era-mixing with a ~10-17% training-size difference | Named here; equalizing train sizes across arms is an open ONE-KNOB run before any strong cross-arm causal claim | this register; L14 audit Q4 | MANUAL — open |
| 56 | A new measurement mechanism that passes its tests and smokes can be trusted to measure | Tests and smokes prove it RUNS; only a known answer proves it MEASURES. L14's era-luck conclusion was reported off the brand-new interlaced instrument before any ground-truth check existed; the planted-signal calibration (tools/planted-signal-check.js) happened after, and only because the owner got suspicious | No new measurement mechanism's output is reported as a finding until it passes a known-answer check (fabricated data with a planted signal AND a pure-noise control, criteria declared first) | tools/planted-signal-check.js (PASS 2026-07-31, 7/7 criteria; its own two failed iterations were the criteria catching bad test data) | MANUAL — the rule; the check itself is runnable |
| 57 | A scattered-window result is a property of the market | It is partly a property of WHERE the blocks landed: across three seeds the 170-setup aggregate went -$5.3k, -$5.9k, +$13.0k (one coin, DOGE, counted 8x by overlapping re-cuts, flips the sign), the top setup went rank 1 -> 38 -> 146, and inter-seed top-10 overlap was 2/10, 4/10, 0/10 | No single-seed board or aggregate may select candidates or be quoted as a finding; scattered-window money is reported with its cross-seed spread; selection credentials must survive placement (multi-seed and/or member-level durability, H1) | classifier-seedcheck-read.sh (declared reads R1-R4; R2/R3 watched failing on real seeds, 2026-07-31) | AUTOMATED for the check; MANUAL as the reporting rule |

| 58 | Weighting rows by DUPLICATING them is a harmless way to implement sample weights | It contaminates validation: walk-forward's recency weighting repeated the trailing 104 weeks of train chunks, and trainMember's last-25% validation slice then held byte-copies of training rows in 100% of folds — lambda under-regularized, boosting ran to its 400-round cap, tau tuned on rows the model had memorized. The planted calibration's B1 failure (the dead coin scoring the same in its alive era, early era and on pure noise) was the visible symptom; the pre-authorized adversarial review localized the cause before any real run | Sample weighting never by repeating rows — only true per-row weights. Any train-set construction must be duplicate-free and provably disjoint from validation | `trainSliceNeverRepeatsOrLeaksAChunk` (test-walkforward.js) | AUTOMATED — watched failing (355 unique !== 710 rows) |
| 59 | A new job kind inherits the operational plumbing automatically | It inherited none of it: walkforward docs (unit progress in `perf`, no `runs` array) vanished from the batch picker (the row builder threw and the doc was silently filtered out), crashed the startup zombie sweep (and the crash aborted the sweep for every doc after it), and were exempt from the audit gate (which matched `bracketlab-` ids only — a whole job kind could have fired unaudited follow-ups). Three independent consumers, all silently wrong | Adding a job kind means auditing every consumer of the doc shape — picker, zombie sweep, audit gate, cancel path, readers. Doc-shape helpers are factored pure and tested | `thePickerListsWalkforwardDocs`, `theZombieSweepSurvivesAWalkforwardDoc` (test-batchdoc.js); require-previous-audit.sh matches `walkforward-` too | AUTOMATED — watched failing (throw / not-iterable with the guards removed) |
| 60 | Clamping or defaulting a malformed number is safe handling | It is how a mis-typed launcher runs five hours at the wrong fee with no error: `feePerLeg: "abc"` became the default and `minTradesSlice: 0` became 1, silently. Same disease QC 43 found for a hard-coded value — this time for a mangled one | Blank means the documented default; malformed means a LOUD refusal naming the field and the received value. The UI sends raw values so the engine is the one place that judges them | `aMalformedNumberIsRejectedNotCorrected` (test-batchdoc.js) | AUTOMATED — watched failing (missing expected exception) |
| 61 | A skip message states a cause it verified | weekly-8d skipped 100% of its walk-forward folds as "slice too thin (data gap)" when no gap existed: week-long chunks can never fill an 8-week slice once the execution purge trims its tail — the geometry is structurally incapable, and the message blamed the data | A configuration that can never score refuses AT LAUNCH with the structural reason; per-fold skip messages name only causes that can actually occur (purge vs gap distinguished) | `weeklyEightDayIsExcludedFromWalkforwardLoudly` (test-batchdoc.js); honest per-fold skip strings in wfUnitTask | AUTOMATED — watched failing |
| 66 | Sliding votes off their dates makes them informationless | It does not: chunks step one day apart and overlap, so any slide that lands within a few days of its origin (a regular occurrence by construction, at both ends of the window) still reads largely the same market — the "zero-knowledge" yardstick was measurably part-informed, inflating the luck baseline and biasing the verdict toward "no better than luck". Caught by the OWNER, from first principles, during the 2026-08-01 debrief. The proposed patch (a keep-out distance) was itself a guessed number and is rejected | The null run's votes are REBUILT, not slid: per fold, each member's real vote mix (same count of ups/downs/stand-asides) is dealt onto random days — zero date knowledge by construction, habits preserved statistically, no distance to assume. The four-arm verdict is PROVISIONAL until re-measured with the rebuilt null | this register; rebuild + re-measure queued for the owner's go | MANUAL until rebuilt — then the planted fake-coin gate must show the rebuilt null destroying the planted edge before any real read |
| 69 | A green test suite means the shipped features work | The 1.32.0 build's three new instruments — the History Tuning launcher, the per-row null replay, the board-verdict reader — were ALL dead or wrong at runtime (scope error, deleted variable still returned, reader never retargeted to the new null construction) while the suite stayed green: no test crossed the orchestration layer. Caught by the owner-ordered adversarial review, three independent lenses, before deploy | Every new launcher/endpoint gets launch-crossing tests that drive it against fake docs on disk; every review probes the wiring layer the suite cannot see | tests/test-htlaunch.js (watched failing — it caught a claim-leak in the fix round itself); the three-lens review practice | AUTOMATED (launch validation) / MANUAL (the review) |
| 70 | Two views of "what data we have" that count differently will be noticed | They were not: the coverage table was taught to count day files (months Binance has not bundled yet) while the sweep read path kept listing whole-month files only — so the screen said "to 2026-08-02" while every all-loaded sweep silently ended June 30. One sweep launched into the mismatch on the strength of the table. The classic instrumentation defect: no error, right units, plausible magnitude | Any change to what the coverage display COUNTS lands in the same commit as the change to what the sweeps READ, with a test asserting the two agree (a day-file-only month must be read by loadSymbolAll) | tests/test-binance.js `dayFileOnlyMonthsAreReadBySweeps` | AUTOMATED — watched failing 2026-08-03 (1 row !== 3: the day files were invisible) |
| 71 | A flag threaded through one stage of a pipeline is threaded through all of them | The null-deal seed (register 66's construction) rode the SLIM stage's payloads and died at PROMOTE: promotionSet rebuilt each unit and dropped it, so every census sweep's null boards trained and voted as REAL arms (bit-identical to the real board), their census rows were tagged real, the five gate arms shared one unit key so model dumps overwrote each other — and the suite stayed green because every test fabricated census rows by hand. Caught by the owner-mandated pre-deploy adversarial review (55 agents, 2026-08-03), NOT by any test | Every arm/flag that shapes a unit's compute must be asserted PRESENT at every stage boundary it crosses, by a test that drives the real stage function with real payload shapes; through-pipeline tests run the worker end to end on the fabricated pair | tests/test-gatepipe.js (promoteStageCarriesTheNullDealSeed; toolOneReplayRunsThroughTheRealWorkerOnFabricatedData) | AUTOMATED — both watched failing 2026-08-03 (seeds [null,null,null] !== [null,1,2]; 'rot is not defined') |
| 72 | A results table fed by every unit is fed only by the units you meant | The replication table took a declared-cell row from EVERY promoted unit — real and all nine null copies alike, untagged — so the cross-coin count (the declared reading's whole point) was computed over a 90%-null population. Caught because the owner questioned a quorum display mid-run and the on-disk record was pulled to answer him | Every stored results row carries the copy tag (nullDealSeed) in the same commit that lets copies write rows; displays filter on it; untagged historical docs use the queue-order inference WITH the real-copy-failed guard, never silently | lib/batch.js replication push tag + app.js repBlock filter (same commit); fallback drops coins whose real copy failed | MANUAL (display-layer; no page harness) — engine tag AUTOMATED once a launch-crossing test exists for doc.replication (open gap, next release) |
| 73 | A capped display list is a safe place to keep authoritative data | It is a LOSSY record: window stamps lived only on the top-50 leader list, so when QC 71's null squatters (or simply rows #51+) pushed a real row past the cap, the one field History Tuning must trust vanished with it — the owner's tuning candidate refused activation a day after passing every read. The numbers survived in the uncapped census; the boundary did not | Anything a later step depends on lives on the UNCAPPED record (census / model dump); capped lists are display only. Census rows now carry windowStamps and census-backed selection passes them through | tests/test-censusselect.js (windowStamps survive census-backed selection) | AUTOMATED for the passthrough; the placement rule is design law |
| 74 | Display limits are an acceptable place to lose computed results | OWNER LAW (2026-08-04, verbatim intent): hours of computed test records must never be deletable, for any reason. Three loss surfaces existed: the top-50 leader cap truncated slim rows outright; discovery runs (no null boards) wrote NO census at all, so promoted results beyond the cap vanished; and QC 73 showed authoritative fields dying with capped rows | Computed records are NEVER deleted. Capped lists are display only. Every promoted unit writes an uncapped census row in every run; every slim unit writes an uncapped compact record; window stamps live on the uncapped record | lib/batch.js census push (unconditional for promoted) + slimResults; tests/test-censusselect.js stamps passthrough | Engine-enforced by construction; the law itself is design law |
| 75 | A write that succeeds is a record that exists | Only if the write cannot be TORN: batch docs (rewritten after every unit), paper-book files, the book registry and the campaign file were all written in place, so a crash or restart mid-write left truncated JSON that every reader silently skipped — a finished multi-hour run could vanish from the picker, and a corrupted live book re-offered the initialize button as if its record never existed (the one thing the paper-book protocol must never do). Found by the 2026-08-04 three-agent review, rated blocker on both counts | EVERY on-disk record is written tmp+rename (atomic on POSIX), same as the candle cache has done since QC 45; unique tmp names per thread+sequence so concurrent writers cannot collide | lib/batch.js saveBatch/atomicWrite, lib/books.js writeRegistry/saveBook, lib/campaign.js, boot zombie-sweep | Engine-enforced by construction (the pattern is the fix); no crash-mid-write harness exists, so the loss mode itself was reasoned, not watched |
| 76 | A setup's name is its trade pair plus geometry plus decision | Context coins are part of the setup's identity: four separate lookups (Tool 2 verdict pairing, the board's vs-nulls column, the leader-to-census dump lookup, menu-grid row matching) keyed on trade|geometry|decision only, so on any run holding a singles row AND a doubles row of the same family, find() returned whichever was pushed first — a silent cross-match between two different machines. The owner's singles-only runs never tripped it; the first multi-size run would have, with no error | Every pairing key carries the FULL setup identity — trade|ctx1|ctx2|geometry|decision(|layout) — in lib/verdict.js keyOf, app.js vnKey and censusRow lookups; null copies are excluded from real-row lookups by nullDealSeed tag | tests/test-inspect.js (keys assert the ctx-slot format), lib/verdict.js keyOf | AUTOMATED for the verdict path (test-inspect watched failing when keyOf changed format); display-layer lookups MANUAL (no page harness — same open gap as QC 63/72) |
| 77 | Two runs with identical settings read identical data | Not under 'all loaded': it reads the whole cache AS OF FIRE TIME, and the cache moves on its own — the 6h bundle refresher banks newly published months, and the live paper books' 30-minute heartbeat, when the keyless mirror is down, falls back to daily zips which are always saved into the shared cache. Owner caught it (2026-08-04) asking why the LTC row sat at 29 instead of 30: his UNI book's fallback had deposited one extra day of UNI candles at 01:57, UNI re-computed on 280 periods instead of 279, its held-back money flipped +$83 to -$11 and it left the neighbourhood. LTC (no book, no new file) reproduced to the last decimal | Cross-run comparisons are valid only with the data pinned: explicit month ranges, or same-day firing, or a per-run cache manifest (proposed, not yet built). Board position is never a stable identifier; the setup name is | lib/manifest.js (1.40.0): every run/null/confirm stamps a content digest of the exact candle files it read; Tool 2 and the compare surface warn by symbol on mismatch; tests/test-manifest.js | AUTOMATED — watched failing 2026-08-04 (content-blind digest reintroduced: 'a changed candle byte must change the fingerprint'). Forensic readers also committed |
| 78 | A reading rule that assigns the audit a check is enough to make the check happen | It is not: the HT combining rule said 'the audit must check the winner is not carried by one split alone'; the audit shipped without it, and the check — run only after the owner questioned the mechanism — found the winner WAS carried by one split (+$222.89 of a +$122.21 total from the middle window alone). A stamped obligation with no mechanical enforcement is a MANUAL item with no owner | Every audit obligation named in a run's stamped rules is computed by the verdict endpoint itself and printed with the verdict (e.g. 'winner carried by one split: YES — middle +$222.89 of +$122.21'), so omission is impossible | to ship with the corrected HT instrument (next HT release); until then the obligation list is read from params.readingRules at audit time | MANUAL — open gap until the endpoint computes it |
| 68 | Retired vocabulary stays retired once corrected | It resurfaced repeatedly in fresh text ("luck committee", "luck rounds", "arm", "slice") — including twice in one day AFTER the owner ordered it fixed for good; four "luck" strings were live on the deployed page itself | Retired words are banned by an automated scan over every user-facing file (page, help pages, workflow, ledger, lib error strings); new terms are defined in the glossary before first use | tests/test-vocabulary.js (watched failing on live page strings and on the design ledger) | AUTOMATED (repo files) / MANUAL (chat discipline — no mechanism can lint conversation) |
| 79 | A summary caveat in a status file can stand in for the primary artifact when judging a past result | It cannot: challenged on whether the H1a decay table was ever valid, the session cited the H1 file's "H1b still owed" line and declared the table "never a valid confirmation... a number without its null" — but the artifact itself (outbox commit 8321e61, 2026-07-31) shows the table SHIPPED with a shuffle floor (+0.001, observed correlations 35-380x above it) and the derived walk-forward design went to explicit owner review before building. The invalidity story, the "caveat didn't travel" story, and the QC 64/65 analogy (money-machinery nulls — a different measurement class from a floored correlation read) were all constructed without pulling the artifact, and QC 79 v1 plus an H1 status stamp were committed on that basis. Caught by the owner demanding the exact table | Any claim about a prior result's validity quotes the primary artifact verbatim — the number, its floor/null, and its audit — before characterizing it; a status-file line about what is "owed" is a pointer to go read, never a verdict to repeat. H1b's true standing: a stricter declared null (scrambled-label rank test), not yet run; its absence leaves the table a strongly-floored lead, NOT an invalidated one | this entry (v2, replacing the mischaracterized v1); corrected status note in reports/H1-DURABLE-MEMBERS.md | MANUAL — quote-the-artifact discipline; no linter can enforce it |
| 67 | Every batch document renders in the research tab's pair-screen picker | Walk-forward docs carry no runs array and none of the screen params; the picker takes the NEWEST batch by default, so the moment a walk-forward doc was newest the research tab threw at first render and showed an error instead of screens — part of what read as "broken interfaces" during the 2026-08-02 rewind conversation | The pair-screen picker lists pair screens only (kind !== 'walkforward'); renderBatch carries a kind-guard BEFORE its header touches doc.runs, so an unexpected kind states what it is instead of throwing | app.js refreshBatch filter + renderBatch guard (d591c18) | MANUAL — no UI test harness exists; the guard has not been watched failing. Open gap: same class as QC 63 (page-boot defects invisible to the node suite) |
| 65 | A paired comparison is fair if both sides apply the same formula | Not if the reference-arm COUNTS differ: the two-arm paired read scored real against the MEAN OF TWO luck arms while scoring each luck arm against ONE other — the smoother reference handed the real arm most of its apparent "+47% lead" (28 vs floor 19). With four arms and near-matched references, real's count (26) fell INSIDE the luck arms' own range (26-38) — the lead was construction, not information | Paired instruments must match reference-arm counts (or bias the asymmetry AGAINST the favoured conclusion, as the four-arm reader does — real vs mean-of-4, lucks vs mean-of-3 slightly favours real, making its null verdict conservative) | classifier-wf4arm-read.sh construction; audit-...-wfnull-s104.md §3 | MANUAL (design rule) — the reader embodies it |
| 64 | Always-going-long is an adequate no-skill control for the walk-forward pick machinery | It is not: on rotated, informationless votes, 57 of 136 setups still "beat always-long" (S1), because the picker chooses the best cell per fold and cells that trade rarely or stand aside beat holding through any falling era — the comparison credits the MACHINERY's option to sit out, not the votes' information. The S1 board made it visible: UNI setups with 0/8 raw support dominated the drift-adjusted ranking purely by not-being-long a falling coin | The null arm (same machinery, rotated votes) is the skill reference for every walk-forward claim; vs-long is a descriptive column only, never a support rule. Any new "skill" metric must be run through the null arm before it may rank anything | classifier-wfnull-read.sh prints null beside real; audit-...-wfnull-s101.md §3 | MANUAL (reporting rule) — AUTOMATED once the paired per-setup real-vs-null reader exists |
| 63 | A UI change that parses and leaves the test suite green is safe to ship | app.js parsed and all ~172 node tests passed while the page would have thrown at first paint (a hoisted function ran at boot and read a `let` still in its dead zone) — bricking every control on the interface, including the only cancel path for the 6-hour job then running. No automated check covers page boot order; the pre-deploy adversarial review caught it, nothing else would have | Any change that touches public/ gets an adversarial review before it ships, explicitly probing boot order and what each new sentence on the screen CLAIMS (the same review caught the banner promising refusals two endpoints do not make). The suite proves the maths; it proves nothing about the page | this register; the review-before-deploy practice | MANUAL — the review is the check |
| 62 | Working silently through the night is fine so long as the work lands eventually | It did not land: serial pacing plus permission-blocked tool calls stalled the whole overnight pipeline, and the owner — promised morning data — woke to nothing and no explanation. The silence, not just the slip, is the failure: he could have re-armed the loop in one message had he known | The moment an expected completion time slips, the standard email goes out saying what slipped, why, and what is being done — before fixing resumes. Known-blocking calls are pre-approved in committed settings so the loop cannot stall on permission again | `.claude/settings.json` permissions.allow (committed 2026-07-31); the email rule | MANUAL (the email rule) / AUTOMATED (the pre-approval file exists and is committed) |
| 87 | A trade's OPEN fee is captured wherever the fee-shrunk quantity already reflects it | For a LONG at a flat price the fee-shrink lowers the exit-proceeds basis AND the entry cost basis by the same amount, so it CANCELS out of `gross` and the entry fee vanishes entirely — realized P&L (the one number the pilot exists to measure) was over-stated by one open-fee per long round-trip. The "already embedded in the qty" comment was the flawed assumption, not the arithmetic | Realized P&L subtracts the recorded entry-leg fee on BOTH sides unconditionally; the recovered-exit path does the same, and a recovered short also books an estimated borrow interest so its P&L cannot be over-stated | mx_executor.py EXIT_FILL paths; test_long_pnl_subtracts_entry_fee, test_recovered_short_exit_books_conservative_interest | AUTOMATED — watched failing |
| 88 | An "I'm alive" event proves the executor's whole cycle ran | Only if it is logged at the END: RECONCILE_OK fires at step 1, BEFORE due exits (step 3), so a box that reconciled then died in the exit loop kept a fresh heartbeat while scheduled exits never fired — the exact failure the watcher exists to catch, masked green. Caught by the round-2 alerting review | The heartbeat set is {RUN_STATUS, BALANCE}, both journaled AFTER the exit loop; identical in the VPS alert and the screen | pilot-alert.sh + lib/pilotview.js; reconcileOkIsNotAHeartbeatButRunStatusIs (test-pilotview) | AUTOMATED for the screen (watched failing); the alert copy is byte-identical MANUAL (no committed pilot-alert harness — open gap, class of QC 63/67) |
| 89 | The watcher can read the master-switch state from RUN_STATUS alone | It cannot: the box is armed by a short-lived `arm` CLI call that journals ARM_SET but emits NO RUN_STATUS, so a box armed and then hung before its next run cycle read `at_risk=false` and suppressed EVERY liveness page — a silent dead-armed box. Caught by the round-2 alerting review | The alert honors ARM_SET/ARM_CLEAR/HALT_CLEAR exactly as the screen does, so `at_risk` cannot read false while the box is armed | pilot-alert.sh replay (ARM_SET/ARM_CLEAR/HALT_CLEAR cases) | MANUAL — verified by fixture; no committed pilot-alert harness (open gap, class of QC 63/67) |
| 90 | A refused START is self-evident to the owner | It was not: a START the box refuses (stale/future request, replay-after-STOP, no secret, bad HMAC) left the screen at "arm pending" forever with no reason shown. Caught by the round-2 arm/liveness review | The four ARM_* refusal events surface as screen incidents, so a refused arm always states why | lib/pilotview.js incident switch; armRefusalEventsSurfaceAsIncidents (test-pilotview) | AUTOMATED — watched failing |
| 91 | The arm freshness check can trust the box OS clock | A skewed box clock (NTP down) made every legitimate START fall outside the freshness window and be SILENTLY refused — the intent-age check had already been moved to exchange time for this reason; the arm gate had not. Caught by the round-2 arm/liveness review | The arm path judges freshness against EXCHANGE-synced time (the `arm` CLI syncs the clock and passes it), falling back to the OS clock only if the sync fails; watermark comparisons stay relative | mx_executor.py honor_arm_request(now_s); test_arm_freshness_uses_provided_clock_not_os | AUTOMATED — watched failing |
| 92 | A mirror-verdict file that exists and says ok proves the drift detector is healthy | A deleted/corrupt verdict, or one that verified nothing (checked:0), all read as green while a live position rode unverified — both the produce-side dead-man and the alert only looked INSIDE `if the file exists`. Caught by the round-2 arm/liveness + alerting reviews | The alert pages on an ABSENT verdict while a position is open, on a CORRUPT verdict, and on checked:0 with a position open; the mirror wrapper writes a fail-closed ok:false verdict if node dies before writing one | pilot-alert.sh mirror block; pilot-mirror.sh fail-closed guard | MANUAL — verified by fixture; no committed pilot-alert harness (open gap, class of QC 63/67) |
| 93 | The mail log is plain text, so grepping it for a message-id and its sasl_username always finds the match | It is not guaranteed text: a single non-ASCII byte in a logged display name or IDN client hostname makes `grep` declare `/var/log/mail.log` a BINARY file and SILENTLY skip it — so the mail verifier found no queue id, and read a genuinely SASL-authenticated owner message (a real Bluemail phone submission carrying sasl_username=theodore) as UNVERIFIED. Every recent message would have failed the same way. The owner sending "just accept my email" is what surfaced it; the danger was the inverse — a spoof and a real mail were becoming indistinguishable (both UNVERIFIED) rather than the check failing safe-closed | The verifier greps with `-a` (--text) so a stray byte cannot blank the whole file; it does NOT relax the rule (the real sasl_username line must still be present for the message-id's queue id). The read-only reader also falls back to the HTML body so a phone-sent (HTML-only) message is readable, not empty | scripts/claude-mail-recent.sh + claude-mail-check.sh (grep -a); claude-mail-recent.sh body_of HTML fallback | AUTOMATED-by-construction (grep -a is the fix) — re-verified live: the owner's message read VERIFIED after the change; no unit harness over the remote log |

| 94 | A fee constant means the same thing to every consumer | REAL_FEE_PER_LEG is 0.125 as DOLLARS on the $100 paper notional (0.125% = 0.00125 as a fraction). The stop tuner works in FRACTIONAL returns and was handed the dollar 0.125 straight, making a 25% round-trip hurdle instead of 0.25% — so only trades that moved >25% counted as winners, and F1 read as winning 2.7% of the time (really 56%). The number had the right units-looking magnitude and got built into a stop value, a both-sides money table, and an emailed conclusion before the OWNER asked "does F1 really lose 97% of the time?" — the exact instrument-defect pattern this register exists for | Any dollar-denominated constant is converted to the consumer's units at the boundary; the stop path divides by NOTIONAL before the fractional tuner, and a regression test pins BOTH the 0.00125 conversion and the direction of the bug (a +0.5% trade is a winner at the real fee, a loser if the dollar value is used as a fraction) | lib/stopsweep.js (feeUsd/NOTIONAL); tests/test-stopsweep.js feeMustBeFractionalNotDollars | AUTOMATED — the test asserts the dollar-as-fraction value flips a real winner to a loser |

Entries 95-103 were all caught by the 2026-08-11 pre-arm end-to-end adversarial
review (four independent lenses: executor, control-plane, stop-math, observability
/ mail), before the box was armed — the standard this register sets: found
unprompted, each with a watched-failing check.

| 95 | A market quote fetched once can safely share its variable name with a per-order fill price | The exit loop assigned each `place()` fill back into `px` — the single market quote EVERY position's fixed-stop check reads. When an earlier due exit returned "unknown" (transport 503, fill None) it clobbered `px` to None and SILENTLY disabled the stop for every LATER position that run; a second position could ride past its stop unhedged. All four existing stop tests used ONE position, so none caught it | The exit fill is kept in a separate `fill_px`; the market `px` is never reassigned in the loop; the EXIT_FILL journal records the fill price | executor/mx_executor.py exit loop; test_earlier_unknown_exit_does_not_disable_a_later_positions_stop | AUTOMATED — watched failing (later position's FIXED_STOP not journaled when px shadowed) |
| 96 | Arm-request tokens are safe to interpolate into the box's remote shell because they are minted server-side | ssh flattens its command into one string the remote shell re-parses, so a nonce/utc/hmac carrying a quote or semicolon could run arbitrary commands ON THE LIVE TRADING BOX — a corrupt or tampered request file was an RCE path to the money box | The three fields are shape-validated (nonce 18-hex, hmac 64-hex, utc ISO-8601) in ONE place before any remote use; anything off-shape degrades to a fail-safe disarm ('0 - - -') that carries no tokens and cannot inject; a disarm needs no tokens and is never gated | scripts/pilot-arm-fields.sh; scripts/test-pilot-arm-fields.sh | AUTOMATED — watched failing (an injection payload passes straight through without the gate) |
| 97 | Any positive stop fraction the owner enters is a usable protective stop | A stop tighter than ordinary hourly noise triggers on microstructure wiggle, not a real adverse move — it turns winners into fee-paying losses and can churn a position out on the first tick | /api/pilot/stop-apply rejects stopPct below a DERIVED 0.5% floor (2x the 0.25% round-trip fee, above LTC hourly-bar noise); the carry helper treats a positive below-floor value as ERROR, never a live stop | server.js stop-apply floor; scripts/pilot-stop-state.sh | AUTOMATED — test-pilot-stop-state (below-floor -> ERROR, not SET) |
| 98 | An unreadable fixed-stop.json means "no stop" | Reading a corrupt or absent-value file as CLEAR let a disk glitch SILENTLY strip a protective stop the owner had set — the box would then trade with no stop and no signal. The old `.6f` format additionally truncated a sub-1e-6 value to a fake "0.000000" the box read as no stop | Three distinct states: SET (valid, emitted at full precision), CLEAR (absent file / null / <=0 = intended no-stop), ERROR (present-but-corrupt / off-shape) — ERROR leaves the box stop UNTOUCHED and surfaces on the pilot screen | scripts/pilot-stop-state.sh; pilot-produce-and-push.sh carry; lib/pilotview.js stopCarryError | AUTOMATED — test-pilot-stop-state (corrupt/empty/non-object -> ERROR, not CLEAR) |
| 99 | The fixed-stop boundary inequality can be weak (<=) | The tuner sets the stop to the deepest MAE any WINNER survived, so the binding winner sits EXACTLY on entry*(1-stop); a weak <= stops that winner out, breaking the "preserve every winner" guarantee at marginFrac=0. The tuner's loser-cut count separately used a mismatched weak >= that over-reported cuts and contradicted its own sacrifice curve | The engine boundary is STRICT (px < / > the level); the tuner's loser predicate matches (mae > stopPct); a position sitting on the boundary is preserved on both sides | executor/mx_executor.py stop_hit; lib/stoptuner.js; test_fixed_stop_boundary_is_strict_long/short; loserExactlyAtStopIsSparedStrictBoundary | AUTOMATED — watched failing (boundary winner cut under weak <=) |
| 100 | The stop tuner and the forward book price the same set of trades | The tuner looked up the exit at the EXACT nominal bar and dropped any entry whose exit bar was missing, while bracket.js (the authoritative forward book) walks <=3h forward over gaps — so near a small data gap the two scored DIFFERENT populations, and the tuner's winner/loser set was not the set actually being traded | entryOutcome walks <=3h forward for the exit, byte-for-byte the bracket.js tolerance, and the MAE window extends to the gap-resolved exit | lib/stoptuner.js entryOutcome; exitGapWithinThreeHoursIsPricedNotDropped / exitGapBeyondThreeHoursIsUnpriced | AUTOMATED — watched failing (a gap-adjacent entry the book counts was dropped as unpriced) |
| 101 | at_risk read from the synced journal is trustworthy when deciding whether to page for a stalled sync | at_risk is derived from the LAST synced RUN_STATUS, but a stalled sync means that reading is itself stale — the box could have armed and opened a position AFTER the freeze, invisible. Gating the "I am blind" alarm on the possibly-blind state is unsound | stale_sync fires on STALENESS ALONE, ungated; dead_heartbeat (box-side liveness) stays at_risk-gated because it reads a different thing | scripts/pilot-alert.sh; scripts/test-pilot-alert.sh (GAP-1) | AUTOMATED — watched failing (a stopped/flat box suppressed the stale-sync page) |
| 102 | Advancing the incident watermark while a page is cooldown-suppressed is harmless, and one ORDER_REJECT kind is enough | Advancing on suppression moved the watermark PAST a genuinely-distinct later occurrence, which then never paged once the cooldown lifted — swallowed. Collapsing ENTRY vs EXIT rejects to one kind let a serious EXIT reject (a position that will not close) be swallowed inside an ENTRY reject's cooldown; the four arm-refusal events did not page at all | A cooldown-suppressed incident does NOT advance the watermark (delayed, never dropped); ORDER_REJECT_ENTRY and ORDER_REJECT_EXIT are distinct kinds; ARM_{HMAC_INVALID,REPLAY_REJECTED,NO_SECRET,STALE_REQUEST} are incident kinds | scripts/pilot-alert.sh; scripts/test-pilot-alert.sh (GAP-3a/3b/4) | AUTOMATED — watched failing (watermark advanced under cooldown; single ORDER_REJECT kind; arm-refusal not paged) |
| 103 | Any queue-id sharing a message-id and showing the owner's SASL proves a genuine owner submission to claude@ | Message-ID is attacker-chosen header text: an attacker reuses a Message-ID the owner legitimately submitted ELSEWHERE and sends their own mail to claude@ under it — the owner's SASL (on the unrelated queue-id) and the claude@ delivery (on the attacker's queue-id) POOL together and "verify" a message claude@ never received from the owner | Verification binds BOTH facts to the SAME queue-id: one queue-id must show the owner's sasl_username AND a to=<claude@...> recipient; the shared helper confirms the recipient in the line itself, not just the upstream grep filter | scripts/mail-verify-parse.py; claude-mail-recent.sh + claude-mail-check.sh; scripts/test-mail-verify-parse.sh | AUTOMATED — watched failing (the collision spoof and an owner-to-third-party both verify under the pooled rule) |

Entries 104-106 were caught by the SCOPED RE-REVIEW of entries 95-103's own fixes
(2026-08-11, same four lenses over the fixes themselves). They are the reason a
fix round gets re-reviewed: one was a real BLOCKER introduced BY a fix. This is
the register's own thesis — a fix is new code and can carry a new defect.

| 104 | Shape-validating the ARM path closes the arm-request injection surface | The DISARM path was left ungated: a clean-parsing armed:false request still emitted its raw nonce/utc/hmac, and those are interpolated into the box's remote shell UPSTREAM of the executor — so a tampered arm-request.json carrying nonce:"';reboot;'" injected at the box's shell-parse layer even on a disarm. "The executor ignores disarm tokens" was the trap; the injection fires before the executor runs. A BLOCKER introduced by the CONTROL BUG 1 fix itself, caught by re-review | pilot-arm-fields.sh emits the token-free disarm sentinel ('0 - - -') on EVERY non-arm, not only on a failed arm — a disarm carries no tokens into the remote shell at all | scripts/pilot-arm-fields.sh (else disarm); scripts/test-pilot-arm-fields.sh (armed:false + injection payload -> sentinels) | AUTOMATED — watched failing |
| 105 | At the tightest stop the binding winner sits exactly on the boundary and is preserved | True in exact arithmetic, but the recomputed boundary entry*(1-mae) lands ~1 ulp ABOVE the winner's low in ~1.6% of float64 price pairs, and the engine's STRICT px < entry*(1-stop) would then stop the very winner the stop is built to preserve. The "marginFrac=0 is SAFE" comment overclaimed a real-number identity that float64 does not honour | tuneFixedStop nudges stopPct up by the minimum ulps that put the binding winner's boundary AT or below its actual low, so the preserve-every-winner guarantee holds in float64, not only in reals; the comment is corrected | lib/stoptuner.js (float64 boundary guard); tests/test-stoptuner.js float64BoundaryGuardPreservesTheBindingWinner | AUTOMATED — watched failing |
| 106 | Matching the mail recipient is independent of how the grep is written | The shell grep that emits the delivery (QTO) lines matched to=<claude@...> CASE-SENSITIVELY while the verdict helper matched case-INSENSITIVELY; postfix logs the recipient localpart verbatim, so a genuine owner message addressed to Claude@... (a saved-contact capitalization) would emit NO QTO line and read UNVERIFIED — a NEW way for real owner mail to fail, introduced by the GAP-2 fix, the exact class ("just accept my email") the fix exists to avoid | both QTO greps use -a -i, matching the helper's already case-insensitive recipient check | scripts/claude-mail-recent.sh + claude-mail-check.sh (grep -a -i) | MANUAL — the helper is case-insensitive and the greps now match it; no harness over the live remote log (open gap G7) |

| 107 | A new sibling script a deployed script calls is deployed along with it | pilot-install.sh installs each orchestrator script to /usr/local/sbin BY NAME. The re-review fixes made pilot-produce-and-push.sh call two NEW siblings (pilot-arm-fields.sh, pilot-stop-state.sh) via its own dir ($HERE=/usr/local/sbin once installed), but the installer was not updated to copy them. The installed pilot-produce-and-push.sh would then call a MISSING pilot-arm-fields.sh, which fail-closes to a permanent DISARM — the box would never arm, invisibly, until the owner pressed START and nothing happened. Caught by tracing the deploy path after the re-review, not by any test | pilot-install.sh installs pilot-arm-fields.sh and pilot-stop-state.sh alongside pilot-produce-and-push.sh; a post-install assertion could verify each $HERE-resolved sibling exists in /usr/local/sbin | scripts/pilot-install.sh (install lines for both helpers) | MANUAL — verified by reading the installer + the $HERE resolution; open gap: no post-install smoke asserts the siblings are present (class of G6) |

| 108 | A boundary guard written for the LONG side covers the SHORT side too | The float64 boundary guard (QC 105) was written with only the LONG formula (entry*(1-stopPct) vs the low). A SHORT binding winner's adverse extreme is a HIGH and it is stopped when px > entry*(1+stop), so its boundary must sit AT or ABOVE that high — the long-only guard left the short binding winner exposed to the same 1-ulp clip. Symmetry was assumed rather than written. Caught while preparing the confirmation pass over the re-review's own fix | the guard is side-aware: LONG nudges while entry*(1-stopPct) > low, SHORT while entry*(1+stopPct) < high; the engine's strict boundary is likewise both-sided (px < / >) | lib/stoptuner.js (clipsBindingWinner); tests/test-stoptuner.js float64BoundaryGuardPreservesTheBindingShortWinner | AUTOMATED — watched failing (short winner clipped under the long-only guard) |

| 109 | The live entry fills at the same price the model trained on | It did not: the trained F1 (and the forward book) enter at the entry candle's OPEN (01:00, "one clear hour after midnight"), but the live producer read that open from the CLOSED-candle cache — which does not hold the entry candle until it CLOSES an hour later. So the live fill landed ~1h past the trained entry, at a price up to the 1% drift-kill away from the decision price — a systematic live-vs-paper execution gap on the very metric the pilot exists to measure. Caught by the OWNER ("we should open on the live market data the open price of the candle... execute within five minutes... follow the same model we trained on") | the producer reads the entry candle's OPEN LIVE (recentKlines through the tunnel) when it is not yet cached — the open is final at the hour boundary and comes from the same source that later writes the cached day-file; ONLY the open is used (features stay closed-only); no live open and no cached open -> WAIT | lib/pilotsignal.js (chooseEntryOpen + live fetch), pilot-produce.js liveOpenFetcher; tests/test-pilotsignal.js chooseEntryOpenPrefersCacheThenLiveThenNull; live smoke pilot-liveopen-smoke.sh | AUTOMATED (unit) + live-smoke verified through the tunnel |
| 110 | Recording a live decision is safe for the mirror whenever it happens | Not under the new record-at-OPEN timing: the producer now records the decision ~1h BEFORE the entry candle caches, so a mirror recompute in that gap found the chunk and full feature window but could not read the +97h entry bar (decision_price=null), and the real-vs-null price rule HALTED the box — on the FIRST live trade of a period, needing owner intervention to clear. A fail-safe direction (no bad fill) but it defeats the instrument the change depends on. Caught by the money-path review of QC 109's fix | computeSignalForChunk flags price_pending when the entry bar is uncached (chunk + feature window present); compareDecision DEFERS only the price check then — side/votes/hash are still verified, and the normal PRICE_TOL check applies once the candle caches. Finding-7 protection intact: a VANISHED feature window still returns found:false and breaks | lib/pilotsignal.js (price_pending), lib/pilotmirror.js (deferral); tests/test-pilotmirror.js pendingEntryPriceDefersInsteadOfBreaking + realVsNullWithoutPendingStillBreaks + pendingPriceStillCatchesSideDrift | AUTOMATED — watched failing (the pending case halts without the deferral) |

| 111 | A protective live-execution guard is safe to add even if the training never had it | Not if it FILTERS trades on a criterion the model never saw: the executor carried a 1% "fill drifted from the decision price" kill (GUESSED, added in the first executor commit) that HALTED the box before entering whenever the market had moved >1% between the decision price and the fill. The training / forward book enters UNCONDITIONALLY at the entry candle's open — it never skips on a price move — so this guard made live take fewer trades than paper on volatile hours: a selection effect on the very fidelity metric the pilot measures, absent from F1's validated numbers. Caught by the OWNER asking "when was that decided, and is that in the training?" | any live gate that can SKIP or HALT a trade must either exist in the training too or be set so loose it only ever catches a broken price, never a real move. FILL_DEV_LIMIT widened to 20% (catastrophic backstop only — a stale/garbage decision_price or flash-crash tier); the fill_deviation is still RECORDED on every entry (the measurement is untouched) | executor/mx_executor.py FILL_DEV_LIMIT=0.20; test_catastrophic_price_drift_halts_without_order + test_moderate_drift_within_backstop_still_enters | AUTOMATED — tests pin that 43% halts but 11% enters |

Entries 112-113 were caught by the SELF-REVIEW of the prime-at-01:00 change
(2026-08-12): entry-aligned timers that fill right at F1's 01:00 boundary so the
live fill lands ~01:01 instead of ~01:10. Infra-only (systemd units, no executor
logic), but a timing change to the money path still gets the four-lens pass over
its own diff.

| 112 | An entry-aligned catch-up timer is harmless — a missed 01:00 fire may as well retro-run when the host comes back | Not for a boundary-locked entry: with Persistent=true, a box that was down at 01:00 and boots at (say) 03:00 makes systemd retro-fire the MISSED entry produce+fill IMMEDIATELY, filling ~2h past the trained 01:00 open. Because the retro-produced intent is stamped at boot time it reads FRESH (not stale), so the 30-min staleness gate does not stop it, and any move within the 20% catastrophic backstop still fills — a late, off-price entry the pilot would misread as an on-time fill. The value of these timers is ONLY at the real boundary; a catch-up past it has none. My first draft carried Persistent=true by copying the regular timers' line | both entry timers (pilot-tick-entry, pilot-exec-entry) are Persistent=false ON PURPOSE — they fire only AT 01:00:15 / 01:01:00 or not at all; the regular Persistent :05 / :00-10 timers remain the sole downtime-recovery path (they already carried that role) | scripts/pilot-install.sh (Persistent=false + rationale on both entry timers) | MANUAL — verified by reading the installed unit; open gap: no harness asserts the deployed .timer files carry Persistent=false (class of G6) |
| 113 | The extra 01:01 entry fire and the regular 01:10 fire cannot double-open the same period | True ONLY because of an ordering the design silently leans on: the executor's arm gate (`if not armed(): return 0`) sits BEFORE the intent loop, so a DISARMED early fire logs no INTENT_SEEN and leaves the intent file untouched — the later armed fire then opens it fresh; and once an ARMED fire opens, it logs INTENT_SEEN + renames the file .done, so the sibling fire (and any duplicate intent the :05 tick shipped for the same chunk_start) hits intent_seen -> INTENT_DUPLICATE and skips. If a future edit moved the INTENT_SEEN log ABOVE the arm gate, a disarmed 01:01 fire would mark the intent seen and the armed 01:10 fire would skip it — the day's entry silently lost. The idempotency is real but load-bearing and undocumented | the invariant is named here: INTENT_SEEN must be logged strictly AFTER the armed() gate and only on an armed run; the chunk_start dedup (intent_seen + .done rename) is what makes the two entry fires idempotent. Verified against mx_executor.py:1064 (arm gate/return) preceding :1119 (INTENT_SEEN) | executor/mx_executor.py (arm gate at 1064 precedes INTENT_SEEN at 1119; intent_seen dedup at 214/1115); executor/test_mx_executor.py test_disarmed_run_preserves_intent_for_a_later_armed_fire | AUTOMATED — watched failing (arm-gate return replaced by pass -> disarmed run logged INTENT_SEEN + ENTRY_FILL, test failed) |

Entry 114 was caught LIVE (2026-08-12) — the owner pressed START on the real
money screen for the first time and nothing happened. It is the canonical
"first-real-use" defect: a POST endpoint that unit tests never exercised because
no test drove the running server over HTTP.

| 114 | A code path that passed review and deploy health checks is exercised | The arm/disarm and stop-sweep POST/GET handlers referenced a bare `fs` that server.js never defines (it aliases the fs module as `dataFs`, line 100), so EVERY POST /api/pilot/arm \| /disarm — and readStopSweep — threw ReferenceError -> HTTP 500 "fs is not defined". The screen's post() does not check response.ok, so the 500 surfaced as a SILENT no-op: the owner pressed START, the confirm dialog closed, and the box never armed because no arm-request.json was ever written. Latent through the entire pre-arm review because these endpoints were only ever first exercised by the first genuine START press on the live money screen — no test drove the running server over HTTP, and `node -c` / deploy healthz (a GET) never touch the arm write path | all bare `fs.` in server.js point at the module's `dataFs`; and a real HTTP test now spins the server up on a throwaway port and drives POST /api/pilot/disarm + GET /api/pilot/stopsweep, asserting 200 + a written request file (the write path the box reads), so a 500/ReferenceError regression fails in CI instead of on the live screen | general-classifier/server.js (dataFs); general-classifier/tests/test-armendpoint.js (disarmEndpointReturns200AndWritesRequestNot500 + stopSweepStatusEndpointDoesNotThrowOnBareFs) | AUTOMATED — watched failing (bare fs reintroduced -> POST returned 500 {"error":"fs is not defined"}, test failed) |

Entry 115 came from the SCOPED HTTP-SURFACE adversarial pass the owner ordered
after entry 114 — driving every pilot endpoint over HTTP with valid + malformed
input, plus auditing the client and the control chain. It is the companion to 114:
114 was the bug, 115 is why the bug was invisible and how the surface is hardened.

| 115 | A control button that POSTs will make its failure visible to the owner | It did NOT: the screen's post() awaited fetch() but never checked response.ok, and fetch only rejects on a NETWORK error — an HTTP 500/400 resolves normally. So every server-side failure of START/STOP (the fs 500, a validation 400, a CSRF 403) closed the confirm dialog and looked like a success while the engine never changed. This is what turned entry 114 from a visible error into a silent no-op that cost a live trading window. The same pass found two more untested-surface gaps: (B) a malformed-JSON POST rendered express's default HTML page WITH a stack trace instead of JSON; (C) arm/disarm ignore the request body and had no CSRF check, so a cross-site <form> POST riding the owner's cached Basic Auth could flip the live-money switch | post() now raises a modal on ANY non-2xx saying the engine state did NOT change (fetch-throws-only-on-network was the trap); a real HTTP test asserts each pilot endpoint's status over the wire. (B) a final JSON error handler returns {error} for parse/uncaught errors — no HTML, no stack. (C) an Origin/Referer guard on arm/disarm refuses any positively cross-site browser request (403, writes nothing) while failing OPEN on absent headers and accepting Origin==Host, so it needs no client change and cannot break the live button | general-classifier/public/pilot.html (post() modal + X nothing swallowed); general-classifier/server.js (JSON error handler; csrfGuard on arm/disarm); general-classifier/tests/test-armendpoint.js (crossSiteOriginIsRefusedAndWritesNothing, sameSiteOriginIsAccepted, originMatchingHostIsAccepted, noOriginIsNotBlocked, malformedJsonReturnsJson400NotHtmlStack) | AUTOMATED — watched failing (guard removed -> cross-site disarm returned 200 and wrote the request, test failed); Fix B + CSRF also live-verified on the running server with the engine left armed and untouched |
| 116 | Sorting greenlights by their timestamp is enough to show "newest first" | Not at millisecond resolution: listGreenlights sorted ONLY on createdUtc (a millisecond ISO string), so two greenlights minted inside the SAME millisecond — two rapid programmatic mints, or the test's two synchronous mints — tied on the sort key and fell through to filesystem readdir order, which is undefined. The Live Trading tab reads the TOP greenlight row as the newest evidence the owner cleared; a non-deterministic tie there can silently show an older decision as the latest. Caught as a flaky test failure (listReturnsNewestFirst) while establishing the Phase-10 green baseline — the same suite had passed by luck on earlier runs | each greenlight record now carries a per-process monotonic `seq`, and listGreenlights tiebreaks equal-createdUtc by seq descending (true creation order); cross-process the millisecond timestamp dominates (real greenlights are minted seconds apart), so seq only disambiguates same-ms same-process mints, where it is exactly right | general-classifier/lib/live/greenlight.js (__mintSeq + record.seq + seq tiebreak in listGreenlights); general-classifier/tests/test-live-greenlight.js (sameMillisecondMintsSortByCreationOrderNotFilename) | AUTOMATED — watched failing 5/5 (seq tiebreak removed -> equal-ms records left in filename order, newer sorted second, test failed) |
| 117 | A deterministic per-(role,chunk) order id is unique enough for crash recovery | Not for schema-2: client_id() truncated its key to the first 14 alphanumeric chars, which held exactly F1's fixed-width ISO stamp but is EATEN by a variable-length setup id. Schema-2 keys are '<setup_id>-<chunk_start>', so a registry auto-generated id ('setup-<ts36>-<hex6>', 19 alnum) collapsed EVERY chunk of that setup to one client id (reproduced: both weeks -> 'f1-entry-setupmdyy8sa1b'). resolve_dangling keys sent/terminated on client_id alone, so after week-1's ACK a week-2 real entry that returned 'unknown' but actually filled shares the already-terminated id, is dropped from dangling, and is NEVER recovered — a silent orphaned real fill (LONG swept for no P&L; SHORT trips the reconcile halt that stops F1 too). Caught by the scoped re-review, empirically reproduced with MockBinance | schema-2 client ids HASH (setup_id, chunk_start) — unique per (setup, chunk, role) within the 36-char venue limit; schema-1 (setup_id is None) keeps the ORIGINAL truncating formula byte-for-byte so F1 ids never change | executor/mx_executor.py (client_id: setup_id branch); executor/test_mx_executor.py (test_schema2_client_ids_are_unique_per_chunk_not_collapsed) | AUTOMATED — watched failing (truncation reintroduced -> both chunks produced 'f1-entry-setupmdyy8sa1b', test failed) |
| 118 | A crash-recovered entry can reconstruct its own hold and stop | Not for schema-2: the recovery reader (resolve_dangling) READS hold_hours/stop_pct/clip_usd from the ENTRY ORDER_SENT meta, but the entry site never WROTE them — the schema-2 entry ctx was only {chunk_start, pos_side, setup_id}. So a recovered generalized entry fell back to F1's HOLD_HOURS=137 and stop_pct=None, riding ~137h STOPLESS against the setup's own declared 48h/5% — violating the owner's every-order-carries-a-stop rule. The reader/writer mismatch (comment claimed the riders 'survive the crash inside the ORDER_SENT meta' — they did not) was latent because no test drove a schema-2 recovery. Caught by the scoped re-review, reproduced end to end | the ENTRY ORDER_SENT ctx now carries hold_hours/stop_pct/clip_usd for schema-2 (schema-1 ctx unchanged, byte-identical); recovery reconstructs the setup's OWN hold+stop | executor/mx_executor.py (entry place() ctx); executor/test_mx_executor.py (test_schema2_entry_order_sent_carries_riders_for_recovery + test_schema2_recovered_entry_keeps_its_own_hold_and_stop) | AUTOMATED — watched failing (riders removed from the entry ctx -> recovered hold_hours None != 48, test failed) |
| 119 | The generalized rail's kills are independent of F1 ("F1 untouched") | They were NOT: the executor's kill machinery was rail-blind. (a) derive() counted every ENTRY/EXIT ORDER_REJECT into ONE box-wide consecutive_rejects and reset it on ANY ORDER_ACK — so a schema-2 setup rejecting 3x halted the box (stopping F1's model-called entries), and schema-2 ACKs interleaving F1 rejects silently diluted F1's own reject kill. (b) the -$50 mark-to-market loss kill summed schema-1 and schema-2 realized+open legs from one pool. (c) a schema-2 pre-order KILL_PRICE_DRIFT and (d) a schema-2 exit reject and (e) a schema-2 catastrophic fill deviation each set the box-wide HALT. Every one violates the owner's cardinal cutover rule "keep both, F1 untouched". Caught by the scoped re-review, each traced to the cited lines | reject counters, realized and the loss/drift/exit/fill-dev kills are SPLIT by rail: consecutive_rejects/realized_s1 count schema-1 (F1) only — byte-identical when no schema-2 event exists — while schema-2 faults set a PER-SETUP halt (HALT-setup-<id>) that blocks only that setup's new entries, never the box and never F1. Per-setup halts gate entries only; open positions still exit | executor/mx_executor.py (derive rejects2/realized2; set_setup_halt/setup_halted; per-rail loss kill; per-setup drift/exit/fill-dev/reject-kill gates); executor/test_mx_executor.py (test_schema2_rejects_do_not_trip_f1_reject_kill, test_schema2_ack_does_not_dilute_f1_reject_streak, test_schema2_price_drift_halts_only_its_setup_not_the_box, test_schema2_drawdown_halts_only_its_setup_not_the_box, test_schema2_exit_reject_halts_only_its_setup_not_the_box) | AUTOMATED — watched failing (each rail-blind variant reintroduced -> the matching guard failed) |
| 120 | The box concurrency cap can be shared across rails | No — MAX_CONCURRENT=6 is sized to F1's own saturation (137h/24h), and the box-wide cap counted schema-1 AND schema-2 real positions, so ONE schema-2 real position at F1's saturation (5 open) made 6 and silently DROPPED an F1 entry the model called — and irrecoverably (the intent is consumed before the cap check). Caught by the scoped re-review | F1's box cap counts ONLY schema-1 positions and applies ONLY to F1 entries (byte-identical when no schema-2 real position is open); schema-2 real setups are bounded by their own per-setup allowlist max_concurrent | executor/mx_executor.py (s1_open cap for schema-1 entries); executor/test_mx_executor.py (test_schema2_real_position_does_not_crowd_out_f1_entry) | AUTOMATED — watched failing (box-wide len(open) cap reintroduced -> F1 entry skipped at 6 total, test failed) |
| 121 | The fail-closed allowlist pins everything about a schema-2 order | It did NOT pin the two most dangerous bits: (a) paper-vs-real — the box decided real vs paper SOLELY from the intent's own `paper` bool; the allowlist carried no paper marker, so a tampered/buggy intent with paper:false for a PAPER-state setup drove REAL margin orders within the clip cap (the one bit deciding real money vs paper was the one the cross-check omitted). (b) hold — hold_hours was unbounded box-side (the 720h cap lived only upstream in configschema, which a tampered intent never traverses), so exit_due_ts could be pinned arbitrarily far out. Caught by the scoped re-review | the producer writes state ('paper'|'live') and max_hold_hours into each allowlist entry; the box decides eff_paper = intent.paper OR allowlist.state != 'live' (a paper-state setup can NEVER place a real order, whatever the intent says; a live setup honors its intent) and refuses hold_hours > max_hold_hours (hold_cap). Fail-closed: an entry without state 'live' is treated as paper | general-classifier/live-produce.js (allowlistFrom: state + max_hold_hours); executor/mx_executor.py (eff_paper from allowlist; hold_cap); executor/test_mx_executor.py (test_schema2_paper_setup_intent_cannot_place_real_order, test_schema2_live_setup_places_real_order, test_schema2_hold_beyond_allowlist_cap_is_refused) | AUTOMATED — watched failing (paper-fill reverted to trust it['paper'] -> a paper-state setup's tampered intent placed a REAL order, test failed) |
| 122 | The F1 screen and F1 alerter read only F1's own events | They did not: both keyed on bare chunk_start with NO setup_id filter, while schema-2 (generalized-setup) events share the ONE journal. So a paper/live setup's INTENT_SEEN overwrote an F1 decision row, its ENTRY_FILL added to F1's open list and leg costs, its EXIT_FILL added schema-2 P&L into F1's realized and popped a shared-chunk F1 open key (silencing a real F1 at_risk page), and its ORDER_REJECT bumped F1's reject streak / paged as an F1 incident. The asymmetry proved it an oversight: the executor's own derive() and the new lib/live/view.js were already rail-safe (setup_id filter); only these two OLD consumers were missed. The plan's 10.4 paper twin produces byte-identical chunk_starts, so the collision is the run's stated goal | both consumers skip any event carrying a setup_id (schema-2), processing only schema-1 (F1) trade events; box-level events (RUN_STATUS/PNL_MTM/BALANCE/ARM_*) carry no setup_id and are still read. The generalized rail has its OWN screen (lib/live/view.js) and alerter (live-alert.sh). Byte-identical to today when no schema-2 event exists | general-classifier/lib/pilotview.js (setup_id skip); vps-access/scripts/pilot-alert.sh (setup_id skip); general-classifier/tests/test-pilotview.js (schema2EventsNeverCorruptTheF1View); vps-access/scripts/test-pilot-alert.sh (R6 incident + at_risk cases) | AUTOMATED — watched failing both sides (filter removed -> schema-2 exit closed the F1 position and a schema-2 reject paged as F1, tests failed) |
| 123 | A greenlighted config means the same thing live as in the lab | Only for the geometry the live executor actually implements. The config vocabulary (validateConfig) accepts the FULL lab search space — breakout entry, the active gate, trailing and arm mults — but the live executor does exactly ONE shape: market entry, hold to tHours, an optional fixed stop. So a greenlighted breakout/active/trailing/arm cell would be SILENTLY traded as market/hold-to-t — a different strategy from the one the lab measured and the owner cleared — with no refusal. Separately, a non-LTCUSDT setup could be minted and set to paper/live though the box hardcodes SYMBOL=LTCUSDT and would reject every intent invisibly. Caught by the scoped re-review (a6f BLOCKER) | going paper/live is gated on liveExecutable(cfg) (entry must be market, gate directional, trailMult/armMult/dMult null — the F1 shape passes unchanged) AND on the target box serving the setup's symbol (mx-1 declares symbols:['LTCUSDT']); a violation throws NOT_LIVE_EXECUTABLE (400 in the UI) and the setup stays a draft, never trading | general-classifier/lib/live/configschema.js (liveExecutable); lib/live/targets.js (box symbols + targetServes); lib/live/setups.js (transition gate for paper/live); tests/test-live-setups.js (f1ShapeSetupMayGoPaperAndLive, unexecutableGeometryIsRefusedAtPaperTransition, unservedSymbolIsRefusedAtPaperTransition) | AUTOMATED — watched failing (transition gate removed -> a breakout/active/trailing cell and a non-LTC symbol both went paper without refusal, tests failed) |
| 124 | An internal run id is safe to path-join into a file read | Not when it arrives from a request: the greenlight endpoint passed req.body.runId straight to getBatch, which does path.join(BATCH_DIR, `${id}.json`) — while the sibling setup/greenlight ids on the same route ARE regex-validated. A traversal-shaped runId ('../../..') therefore path-joined out of the batches dir into an arbitrary .json read. Caught by the scoped re-review | getBatch validates the id shape (a letter/digit then letters/digits/._- , and no '..') and returns null for anything else, so a traversal id reads nothing — guarding every caller, not just that route | general-classifier/lib/batch.js (getBatch id guard); general-classifier/tests/test-batchdoc.js (getBatchRefusesPathTraversalIds) | AUTOMATED — watched failing (guard removed -> getBatch read a sentinel .json outside the batches dir, test failed) |
| 125 | Two setups (or a setup and F1) can safely trade real money on one sub-account | No — one isolated-margin sub-account pools ALL borrow and balance, so multi-day short interest accrues on the shared pool and, with per-leg nominal repayment, lands entirely on whichever short closes LAST regardless of rail; per-setup realized (view.js sums EXIT_FILL pnl by setup_id) then inherits the misattribution, and F1 can absorb a schema-2 short's interest or vice versa. It is physically unfixable by after-the-fact accounting while the account is shared. Caught by the scoped re-review (aggregate box realized stays correct; the corruption is per-setup/F1 attribution) | going LIVE requires the setup's OWN sub-account keyRef, enforced at the paper/live transition, so no two live setups ever share a borrow/balance pool; PAPER places no orders and needs none. F1's short money-math is UNTOUCHED (no live setup can share its account). The executor does not yet ROUTE per-setup keyRef (open gap G8) — until it does, this gate is what prevents the shared-pool scenario from arising | general-classifier/lib/live/setups.js (transition: live requires keyRef); tests/test-live-setups.js (liveRequiresItsOwnSubAccountKeyRef) | AUTOMATED — watched failing (keyRef gate removed -> a setup went live with no sub-account, test failed) |
| 126 | Paper books measure regardless of the master switch | They did not: PAPER_ENTRY_FILL was emitted only inside the intent loop, which the arm/halt/reject-kill early-returns skipped entirely — so a disarmed, halted or reject-killed box silently stopped booking paper ENTRIES (paper exits already ran in every state; entries did not), and paper intents older than 30 min were then discarded to .bad. A real-money fault thus silently blanked the very paper measurement the pilot's fidelity comparison depends on. Caught by the scoped re-review | the entry gate sets a real_blocked flag instead of returning; a PAPER intent (no order) is processed in every state while a REAL intent under real_blocked is LEFT UNTOUCHED for a later armed run (paper-ness read from the allowlist BEFORE validation, so a blocked real intent gets no side effects) — byte-identical for a disarmed/halted F1 box (no paper intents; real intents untouched; no BALANCE) | executor/mx_executor.py (real_blocked + light pre-gate + conditional balance snapshot); executor/test_mx_executor.py (test_paper_intent_books_even_when_disarmed) | AUTOMATED — watched failing (disarm restored to early-return -> paper intent never booked while disarmed, test failed) |
| 127 | The setup screen keeps real and paper money separate | Realized was separate (realizedPnl vs paperRealizedPnl) but UNREALIZED was not: view.js summed unrealized over [...open, ...paperOpen] into one number, and after a legal paper->live transition a setup can hold BOTH books open at once — so the Unrealized tile showed real + fictional paper money added together, unlabelled. Caught by the scoped re-review | unrealized is split into unrealizedPnl (real) and paperUnrealizedPnl (paper), exactly as realized already is; the tab shows the setup's OWN book (real for a live setup, paper for a paper setup) and each open row keeps its paper badge | general-classifier/lib/live/view.js (sumUnreal per book); public/livetrading.html (own-book unrealized tile); tests/test-live-view.js (paperAndRealUnrealizedAreSeparate) | AUTOMATED — watched failing (unrealized re-summed over both books -> 6.0 instead of the real-only 2.0, test failed) |
| 128 | The generalized rail's per-setup alerting works | Mostly dead code: the executor journaled the alertable schema-2 incidents (EXIT_OVERDUE, INTENT_STALE, INTENT_INVALID) WITHOUT setup_id, so live-alert.sh's `if not setup_id: continue` filter dropped them (allowlist-refusal alerts could NEVER fire) — and separately live-alert never checked mirror.json freshness, so a STALLED mirror/producer read as "fresh" and hid its own silence (fail-open). Caught by the scoped re-review | the executor carries setup_id on EXIT_OVERDUE/INTENT_STALE/INTENT_INVALID (and already on KILL_PRICE_DRIFT per R1), so live-alert sees them and pilot-alert's R6 filter skips them; live-alert also pages MIRROR_STALE when mirror.json exists but has not been refreshed in MIRROR_STALE_S (writer ~hourly) | executor/mx_executor.py (setup_id on the incidents); scripts/live-alert.sh (mirror-staleness page); scripts/test-live-alert.sh (PASS 5 stale mirror) | AUTOMATED — watched failing (stale-mirror check removed -> a 4h-old mirror.json paged nothing, test failed) |
| 129 | An alert that pages once per cooldown is not spam | It was: live-alert.sh keyed the cooldown on the PAGED WALL TIME, so a single schema-2 incident sitting in the 26h lookback window re-paged every cooldown (hourly) for the whole day — 26 emails for one event. Caught by the scoped re-review | each journal incident carries its own ts; live-alert keeps the LATEST per (setup, kind) and pages only when that ts is NEWER than the per-key watermark (page on transition), while mirror/health alerts keep the wall-clock cooldown. A genuinely later occurrence still re-pages | scripts/live-alert.sh (per-key incident-ts watermark); scripts/test-live-alert.sh (PASS 4: a 2h-old already-paged incident does not re-page though the cooldown expired; a newer one does) | AUTOMATED — watched failing (watermark reverted to timer-only -> the stale already-paged incident re-paged, test failed) |
| 130 | The execution-fidelity average measures one population | It mixed three: view.js pushed EVERY fill_deviation into one `fills` array — real fills, RECOVERED fills (which fabricate deviation 0.0 because the decision price was lost in the crash), and PAPER fills (the lab twin, not live execution). So the headline "is live faithful?" number was diluted by fabricated zeros and muddied by paper measurements — the exact metric-substitution class the QC register exists to stop. Caught by the scoped re-review | fidelity.fills / fillDeviationAvg / fillDeviationMax count REAL, non-recovered fills only; recoveredFills and paperFills (with paperFillDeviationAvg) are reported in their own buckets so neither dilutes the real number | general-classifier/lib/live/view.js (separate fills/paperFills buckets + recoveredFills); tests/test-live-view.js (fidelityCountsOnlyRealNonRecoveredFills) | AUTOMATED — watched failing (recovered + paper re-pooled into fills -> 3 fills instead of the 1 real, test failed) |
| 131 | The producer can ship intents independently of carrying the allowlist | It should not: live-produce-and-push.sh carried the allowlist and then shipped intents UNCONDITIONALLY — so on an allowlist-carry failure (box keeps its previous list) a fresh intent shipped to act against a STALE box allowlist, the exact thing the allowlist exists to prevent. Separately both scp writes were non-atomic — the box's fail-closed reader / intent loop could read a half-written allowlist or intent. Caught by the scoped re-review | the allowlist and each intent are scp'd to a dot-temp then mv'd into place on the box (atomic; the intent loop matches only *.json so the temp is invisible), and intents are shipped ONLY when the allowlist carry succeeded this tick (else they wait in the outbox and retry together next tick) | vps-access/scripts/live-produce-and-push.sh (allow_ok gate + atomic scp+mv) | MANUAL — verified by reading; no harness over the live scp/ssh boundary (same class as gap G7). First real tick after the box deploy is the check |
| 132 | An intent, once seen, is safe to consume before the entry checks | Not on a TRANSIENT failure: INTENT_SEEN + the .done rename (the dedup that stops a re-emitted intent) were recorded BEFORE the price fetch, so a run that could not read the price (a transient ticker hiccup) journalled INTENT_SEEN, renamed the file .done, then ENTRY_SKIPPED — and the intent_seen dedup made the next run treat it as a duplicate. One exchange blip therefore permanently forfeited the entry the model called. Caught by the scoped re-review. FOLLOW-UP (inline self-review): the first fix applied the pre_price guard to BOTH rails, silently changing the LIVE F1 (schema-1) pilot's price-fetch ordering — a behaviour change to a running live-money path, violating the F1 byte-identity invariant the owner flagged. R18 is therefore SCOPED to schema-2 only | schema-2: the price is fetched BEFORE INTENT_SEEN/.done (non-FLAT), and a no-price run leaves the intent as .json for the next run to retry; the fetched price is reused for the order. schema-1 (F1) is UNTOUCHED — `if is2 and it["side"]!="FLAT"` gates the guard, and `price = pre_price if is2 else bx.price()` restores F1's original fetch AFTER INTENT_SEEN/.done, so a no-price F1 run consumes the intent and skips the period exactly as the live pilot always has. QC-113 preserved — arm gate and dup check still precede this | executor/mx_executor.py (is2-gated pre_price guard; is2-conditional reuse at order); executor/test_mx_executor.py (test_transient_no_price_keeps_the_intent_for_retry for schema-2 retry; test_transient_no_price_leaves_F1_byte_identical as the F1 tripwire) | SUPERSEDED 2026-08-16 BY OWNER DIRECTIVE, see QC-141 — the F1 scoping was the deliberate choice to keep the live pilot byte-identical, and its price of a forfeited period is exactly what the owner ruled out. R18 now applies to BOTH rails; the F1 tripwire test (test_transient_no_price_leaves_F1_byte_identical) was REPLACED by test_transient_no_price_keeps_the_F1_intent_for_retry, which pins the new intended behaviour. The schema-2 half of this item stands and is still AUTOMATED |
| 133 | The mirror's newest-10 window and paper/live filter cover the drift surface | They did not: the per-setup mirror recomputed only the newest 10 decisions and ran only over paper/live setups. A setup with a longer hold than 10 chunks left older-but-still-OPEN positions' decisions unverified, and a STOPPED setup (new entries halted, but existing positions still exiting) was never mirror-checked at all. Caught by the scoped re-review | the recompute window covers the whole hold (max(10, ceil(tHours/24)+10)) so every unclosed position's decision is re-verified, and the mirror sweep includes stopped setups (only draft/retired are skipped) | general-classifier/lib/live/mirror.js (config-derived window); general-classifier/live-mirror.js (include stopped); tests/test-live-mirror.js (mirrorWindowCoversTheHoldNotJustNewestTen) | AUTOMATED — watched failing (window reverted to flat 10 -> only 10 of 15 decisions checked, test failed) |
| 134 | Terminal-state files are harmless once written | Harmless but UNBOUNDED: shipped intents (outbox *.json.shipped) and the box's terminal intent files (*.done/*.dup/*.bad) are inert once written but accumulate forever, so the outbox and the box's intents dir grow without limit over a long run. Caught by the scoped re-review | the tick script prunes those terminal-state files older than 7 days on both sides (best-effort, never a tick failure); it touches ONLY already-terminal files — never a live .json intent or a decision log — so it cannot affect trading | vps-access/scripts/live-produce-and-push.sh (7-day find -delete of *.shipped locally and *.done/*.dup/*.bad on the box) | MANUAL — verified by reading + bash -n; no harness over the live find/ssh boundary (class of G7); the prune is bounded to terminal suffixes only |
| 135 | The allowlist 'state:live' pin is enough to safely run a REAL schema-2 order on the F1 box | It is NOT: the executor signs EVERY order with the box's single ~/.executor-env key (gap G8), so a REAL schema-2 order lands on F1's OWN isolated-margin LTCUSDT wallet + borrow pool. That re-couples the rails through shared reconcile-halt (reconcile sums both rails' open legs and sets the BOX halt on any mismatch) and shared short-close sizing (is_last_short counts across rails, so a schema-2 short changes the buy-back qty of F1's own short) — impossible to rail-split on one wallet. The classifier's transition gate (QC 125) refuses live-without-keyRef, but the EXECUTOR never checked, so a hand-edited allowlist 'state:live' was one line from a real order on F1's account. Caught by the independent post-fix review (2026-08-12), corroborated by a second reviewer (uncapped real exposure N1 + cross-rail interest N2) | schema-2 is PAPER-ONLY on the F1 box in code: S2_LIVE_ROUTING=False forces a would-be-real schema-2 intent to paper (it still measures, places NO order) and logs S2_LIVE_UNSUPPORTED loudly. Applied at BOTH the blocked-path pre-check and the main eff_paper. F1 (schema-1) untouched. This also removes uncapped real schema-2 exposure (N1) and cross-rail interest (N2) until G8 lands — the flag flips True only in the same change that adds per-setup key routing AND a box-wide real-exposure ceiling | executor/mx_executor.py (S2_LIVE_ROUTING guard); executor/test_mx_executor.py (test_schema2_live_is_refused_paper_only_on_the_f1_box) | AUTOMATED — watched failing (S2_LIVE_ROUTING flipped True -> a real order appears for a state:live schema-2 setup, test failed) |
| 136 | The R8 intent loop runs in every arm state with no side effect on a blocked REAL intent | Only for a WELL-FORMED intent: a corrupt/torn intent is parsed and set aside (.bad) at the top of the loop BEFORE the real_blocked gate, so a torn F1 intent arriving during a disarm/halt window was consumed (period forfeited) where the pre-R8 early-return left it untouched. Worse, the .bad os.rename was unguarded, so a file that vanished mid-run (a listdir/open race) raised FileNotFoundError and aborted the whole run — F1's own scheduled exits included. Caught by the independent post-fix review | while the REAL rail is blocked a parse failure is LEFT untouched (retried next armed run) — byte-identical to the pre-R8 early-return; and the set-aside rename is wrapped so a vanished file can never abort the run | executor/mx_executor.py (real_blocked no-touch on parse failure; guarded os.rename); executor/test_mx_executor.py (test_blocked_rail_leaves_a_corrupt_f1_intent_untouched, test_vanished_intent_file_mid_run_does_not_abort) | AUTOMATED — both watched failing (drop the no-touch guard -> a disarmed run .bad's a torn intent; drop the try/except -> the vanished-file race aborts do_run) |
| 137 | The transition door is the only way into a trading state, so gating it is enough | No — updateSetup can mutate executionTargetRef and keyRef (the routing/isolation fields) and ran only shape validation, so an already-trading setup could be re-pointed at a box that does not serve its symbol, or have its keyRef blanked (re-mingling its borrow/balance pool with F1) — a SECOND unguarded door into the exact states transition() refuses. Caught by the independent post-fix review; a CONFIRMING re-review then caught that the first fix covered paper/live but MISSED 'stopped' — a stopped setup still EXITS real open positions routed by executionTargetRef, so a bad re-point would silently misroute a live exit (a THIRD door) | the live-executability gate is factored into liveGateErrors() and re-run by updateSetup whenever the setup is paper/live/STOPPED, so a routing/keyRef change must re-clear the same gate; passing s.state means stopped is checked for target/geometry without spuriously demanding keyRef; name/clip/stop stay tunable; draft/retired excluded | general-classifier/lib/live/setups.js (liveGateErrors shared by transition + updateSetup, paper/live/stopped); tests/test-live-setups.js (updateSetupReRunsTheLiveGateSoRoutingCantBeBlankedPastTheDoor, updateSetupGateAlsoCoversStoppedSetupsWindingDownRealExits) | AUTOMATED — both watched failing (gate removed -> blanking a live setup's keyRef accepted; 'stopped' dropped -> re-pointing a stopped setup at a dangling target accepted) |
| 138 | A missing local allowlist file means 'nothing to carry' | It meant fail-OPEN: with $ALLOW absent the carry `if` body was skipped, allow_ok stayed 1, and intents shipped this tick against whatever STALE allowlist the box still held — exactly what R14's carry-gate exists to prevent. Caught by the independent post-fix review | a missing local allowlist now sets allow_ok=0 (fail-closed): no intents ship until a real allowlist is produced and carried | vps-access/scripts/live-produce-and-push.sh (else-branch on the `[ -f $ALLOW ]` test) | MANUAL — verified by reading + bash -n; no harness over the live scp/ssh boundary (class of G7) |
| 139 | Deployed = shipped: if the files are on the server, the feature is delivered | No — the phase-5 Live Trading UI (livetrading.html) was deployed by every deploy since 2026-08-12 morning, but NOTHING linked to it: no tab in the app, no button anywhere, and the page had no site header back. From the owner's side the release was invisible ("where's the new code?", two mails), and three review rounds missed it because every lens checked the money path, never "can a human navigate to this screen from the home page". Caught by the owner | the tab bar now links Live trading (next to Live pilot) and the page carries the site nav; and the release rule is amended: a UI-facing feature is not shipped until it is REACHABLE — a navigation path from the home page is part of the deliverable, checked before the change-email is sent | general-classifier/public/index.html (tabbar link); general-classifier/public/livetrading.html (nav header); this register (release rule) | MANUAL — navigation reachability is a human-path property; enforced by the release rule in this entry and the change-email checklist, no automated harness yet |
| 140 | A record can be dated by the key it is stored under | Not when the key is not the event: a decision is KEYED on the start of its 96h feature window but ACTS 97h later, so the screens' date column showed a window start four days and an hour before the trade it drove. A complete, current record — four decisions, one per day since arming — read as 'no decisions for the past 4 decision days'; the owner asked what had broken. Nothing had. A label made a healthy live rail look abandoned, which is the same class of defect as a wrong number: it drives a decision. Caught by the owner 2026-08-16 | pilotview.status() stamps every decision row with entry_utc = chunk_start + entryOffsetH (geometry from config(), F1 97h fallback, null on an unusable stamp — never a silent fall back to the window start); both history tables date rows by entry_utc and show the window start only as muted provenance | lib/pilotview.js (decisionEntryUtc + the status() stamping); tests/test-decisiondate.js (decisionsCarryTheMomentTheyAct against a synthetic journal; bothScreensDateRowsByTheActingEntry bans a chunk_start leading cell in every listed screen) | AUTOMATED — watched failing 2026-08-16: removing the status() stamping fails the synthetic-journal test, and restoring either table's `${(dec.chunk_start||'').slice(0,10)}` leading cell fails the screen test |
| 141 | A failed entry is a lost period — one attempt is all an entry gets | It was, and silently: the intent was consumed (INTENT_SEEN + .done) BEFORE the price fetch and the order, so any failure after that point — no price this run, a rejected order — forfeited the trade the model called, with nothing on the screen saying a trade had been missed. The pre-consumption failures fared little better: INTENT_MAX_AGE_S 1800 gave three 10-minute ticks and then killed the intent. Owner directive 2026-08-16: a failed entry may try again up to 6 times, until one hour past its moment, then the period is abandoned OUT LOUD | The entry retry protocol. The intent file is kept until the period reaches a TERMINAL state (entry_terminal: real fill, paper fill, ENTRY_INFLIGHT, drift kill, ENTRY_GAVE_UP); attempts are counted from ENTRY_ATTEMPT events in the journal, capped at ENTRY_MAX_ATTEMPTS 6; the window is ENTRY_RETRY_WINDOW_S 3600 anchored on the producer-stamped entry_ts (not the mint stamp, so a late-minted intent gains no extra life); one attempt per period per run; INTENT_SEEN stays one-per-period so the decision row is not doubled; ENTRY_GAVE_UP is an incident on the screen and a page from the alerter. The one-fill invariant is carried by entry_terminal(), not by consuming the file early, and every attempt reuses the same deterministic client_id so a resend the venue already filled is refused as a duplicate | executor/mx_executor.py (entry_terminal, entry_attempts, entry_deadline, the attempted_this_run guard, _finish at each terminal outcome); executor/test_mx_executor.py (11 checks); general-classifier/lib/pilotsignal.js (entry_ts stamp); general-classifier/lib/pilotview.js + scripts/pilot-alert.sh (ENTRY_GAVE_UP surfaced and paged) | AUTOMATED — watched failing 2026-08-16: 7 behaviour checks fail or error against the pre-change executor (retry-then-fill, budget spent, give-up terminal, window closed, past-the-old-bound now trades, F1 keep-for-retry, entry_deadline) and the same-run guard test fails with the guard disabled. 4 further checks are REGRESSION GUARDS on invariants that must hold on both sides (no double fill, no retry behind an inflight order, FLAT one-shot, inside-the-old-bound still trades) and pass either way by design |
| 142 | A statistic may be used to ORDER a list even where the register bans it as evidence | It may not: an ordering IS a claim about which row is better, so a banned statistic in the sort key makes the claim the ban exists to prevent — silently, because nobody reads a sort order as an assertion. QC-7 already said 'never quote a binomial p as evidence; units are correlated, the measured null is the only yardstick'. On 2026-08-17 the Constructing replication list shipped ranked PRIMARILY on a binomial p across assets, with the p-value written into its own key text as the reading. The rule was already on the books and nothing stopped it: no check looks at what a sort key is made of. The owner caught it by asking why other assets should say anything about this one | A ranked list may sort only on statistics the register admits as evidence. Where a banned statistic is genuinely informative it may be DISPLAYED, labelled as context, never used as a sort key. The replication list leads on the measured null (real vs its own dealt-vote copies), then plateau width on the traded asset, then the across-asset share as context | tests/test-declaredset.js noSortKeyIsBuiltFromAStatisticTheRegisterBans — reads the replication ranking's own comparator and refuses a binomial, a p-value or a p floor inside it, then asserts the admitted statistics appear in the required order (measured null, then plateau width, then money LAST). Companions: theBannedStatisticIsNotComputedAtAll (the binom helper is gone, not merely unused — dead code producing a banned statistic is an invitation to quote it) and everyRankedColumnCarriesItsReadingRule (each column states its rule, since these four are misread in opposite directions if swapped). NOT banned and explicitly protected by the same test: the 1-in-(N+1) RESOLUTION FLOOR of N measured null draws, which is the register's own sanctioned framing and a different quantity from a binomial across correlated assets | AUTOMATED — watched failing 2026-08-17: restoring `a.p - b.p` as the first sort key fails it, which is precisely the defect that shipped; promoting money above plateau width fails it; dropping the CONTEXT label off the across-asset share fails the companion. The ranking now leads on the measured null (the configuration against its OWN dealt-vote copies), then plateau width on the traded asset, then the across-asset share as context, then money |
| 143 | Reading the source is enough to know a page works | It is not, and this repo had nothing else. `draw()` returned undefined while every section function it dispatches to is async, so "copy settings into the form" — which does `draw().then(fill)` to wait for the Sweep form to exist — threw on every single click. The tab switched, so from the owner's chair the button LOOKED like it had worked; the exception went to a console nobody was reading and not one field was ever filled. No grep can see that, and no reviewer reading either half in isolation would: both halves are correct on their own. The whole class — a handler that throws on a missing element, a render that dies mid-panel, a request built with half a key — is invisible to every static check, and every check this repo had was static | a RUNTIME harness that starts the real server, opens both pages in Chromium and walks 44 views: every tab with nothing open, every tab with a saved run open, every tab clicked through (disclosures expanded, rows clicked, dropdowns cycled through all options, buttons pressed except launchers and destructive ones), plus the light palette. It fails on an uncaught exception, a console error, a failed or 4xx request, and on undefined/NaN/[object Object]/Infinity reaching the visible text. It REFUSES to run when the box has no finished bracket-lab run, rather than checking empty states and reporting a clean bill | general-classifier/tests/browser.js (`npm run test:ui`); general-classifier/tests/test-uicontracts.js (everySectionIsReachableFromDrawAsAPromise pins the specific fault at source) | AUTOMATED — watched failing 2026-08-17: reverting one branch of draw() to a bare call fails the source test, and the harness reported the live exception on Constructing/boards before the fix and stopped reporting it after. Deliberately NOT in `npm test`, which must stay dependency-free and network-free for the deploy path |
| 144 | A field the front end sends is a field the server receives | Second instance, and this one cost more than the first. lib/verdict.js has keyed a setup by trade\|ctx1\|ctx2\|geometry\|decision since the 2026-08-04 review — precisely so a singles+doubles run cannot score whichever same-family row was stored first — and BOTH pages sent the context pairs correctly. The HTTP route built its selection from trade/geometry/decision and silently dropped ctx1/ctx2, so every request arrived as the SINGLES key and came back "setup … not in this run's real rows". Since the live vocabulary only accepts three-asset combos, the null verdict — the check that says whether a result beats its own null — was unreadable for every setup this project can actually trade, on both pages, for as long as the route has existed. Same shape as the declaredPermute drop caught 2026-08-17 (form offers it, caller sends it, server drops it) | the route forwards ctx1/ctx2 into the selection; the Constructing tab sends them; and the layout is taken from the ROW's own arm rather than the run's params, because verdict.js fills a single-arm doc in from its rows and only a mixed-arm run needs the arm named | general-classifier/server.js (null-verdict route sel); general-classifier/public/constructing.js (Tool 1 request); general-classifier/tests/test-verdictctx.js (aThreeAssetSetupIsFoundByItsOwnKey, askingWithoutTheContextsDoesNotMatchAThreeAssetRow, theRouteForwardsTheContextPairs, theConstructingTabSendsTheContextPairs); vps-access/scripts/classifier-verdict-check.sh asks the DEPLOYED service for a real three-asset verdict, because the cut wire is between two correct ends and no grep over either end can see it | AUTOMATED — watched failing 2026-08-17: deleting the ctx1 line from the route fails the route test; removing ctx from keyOf fails both library tests. The live check passes on the box against four of the owner's own historical triples runs |
| 145 | Asking the operator to type an identifier is an acceptable way to collect it | Not when the service can list the valid ones. Tool 1's scramble run and Compare's run A/B were free-text boxes, so an empty box or a typo came back as an HTTP 400 for the operator to decode — "unknown scramble run" when they had simply not chosen one. The endpoint that lists exactly the eligible runs, with a null-draw count on each, has existed the whole time and this tab never called it. The owner had already named this class once, on the stop tuner's target: "I thought I would be able to select from the saved sweeps" | every run-id control is a dropdown fed by the server's own list, filtered to what the tool can actually read (null draws for Tool 1, real rows for Compare); an empty, self-referential or impossible pick is refused on the page in plain words instead of being posted for the server to reject | general-classifier/public/constructing.js (t1null/cmpA/cmpB selects + client-side refusals); general-classifier/tests/test-uicontracts.js (runIdsArePickedFromTheServersListNeverTyped, theTabActuallyCallsTheEndpointThatFeedsThePickers, anEmptyPickerRefusesInPlainWordsInsteadOfAsking); vps-access/scripts/classifier-sweep-verify.sh (the three ids must be `<select`, never `<input`, on the deployed page) | AUTOMATED — watched failing 2026-08-17: putting `<input id="t1null">` back fails the picker test, and deleting the verdict-sources call fails the endpoint test |
| 146 | The STOP half of a two-state control is safe if the START half works | It is not, and this was the real-money master switch. The Trading tab routed BOTH buttons to /api/pilot/arm and encoded the difference in a body field {armed} that the route has never read — it hard-codes writeArmRequest(true). So STOP was not a no-op, it was a RE-ARM: writeArmRequest mints a fresh nonce and utc on every call and the box edge-triggers on that as a genuine START. The operator confirmed "STOP the F1 engine?", got no error, and the screen redrew as RUNNING because pilotview computes armPending as req.armed !== st.armed, which is false when both are true. Real money kept trading while the switch reported it was off. public/pilot.html has called the two routes correctly since it existed, so a reviewer comparing the two pages would have seen it; nobody did, and no test drove the STOP path at all | the page picks its ROUTE from the button pressed (arm vs disarm), and the ARM route REFUSES a body carrying armed:false with a 400 naming the disarm route — so a future caller cannot repeat this silently on the one control that stops real-money trading. A contradictory request now fails where the operator can see it, instead of being answered with its opposite | general-classifier/public/trading.html (route chosen from the button); general-classifier/server.js (arm route refuses armed:false); general-classifier/tests/test-armendpoint.js (theArmRouteRefusesAContradictoryArmedFalse drives the real endpoint over HTTP; theStopButtonPostsToTheDisarmRoute pins the page) | AUTOMATED — both watched failing 2026-08-17: restoring post('api/pilot/arm',{armed}) fails the page test, and removing the server guard makes the endpoint answer 200 and write armed:true to a disarm request |
| 147 | A control that is offered is a control the backend accepts | Three shipped controls on the Constructing tab could not do anything at all. The Greenlight anchor "widest region" — the one option whose entire purpose is to resist shopping — was refused by lib/live/greenlight.js, which accepted only 'best' and 'declared'; the two anchors that DID work are the two its own tooltip warns flatter themselves. "No stop (clear)" sent 0 where the endpoint refuses anything <= 0, so the protective stop could not be cleared from that tab at all. "Fire null runs on this run" fired the ROTATION null while sitting inside Tool 1 claiming to feed it, and its output (doc.nullTest) was rendered NOWHERE on the tab, so each round cost a full sweep and displayed nothing | each control was made to work or made honest: the greenlight door takes the region anchor and refuses BY NAME when a row has none (a silent fallback would mint the shopped cell under the anti-shopping label); clear sends null and the endpoint's comment no longer claims 0 works, with 0 still refused on purpose because an empty box parses to 0 and a parse slip must not strip a live risk parameter; the rotation rounds are their own panel, named for the instrument they are, output rendered, stating that the register retires the construction as evidence and that they are not what Tool 1 reads | general-classifier/lib/live/greenlight.js; general-classifier/public/constructing.js; tests/test-live-greenlight.js (regionTargetUsesTheRegionCentreNotTheSearchWinner, aRowWithNoRegionRefusesRatherThanFallingBackToTheWinner — FUNCTIONAL, not grep); tests/test-verifytune.js (rotationRoundsAreTheirOwnInstrumentAndTheirOutputIsShown) | AUTOMATED — watched failing 2026-08-17 with each control's fault reintroduced. RELATED FINDING, recorded here because it is the reason this went unseen: a test of mine was NAMED greenlightAcceptsTheRegionTarget... while it grepped lib/batch.js, the CONFIRM door — a different function in a different file. It was green for the whole time the greenlight refused that anchor. A test named after a door it does not open is worse than no test: it is a false all-clear. Renamed to confirmAccepts..., extended to the greenlight module, and the greenlight door now has a functional test rather than a grep |
| 148 | A ranked list is ordered by the statistic named at the top of it | The Constructing replication ranking led on the measured null — the configuration against its OWN dealt-vote copies, the only yardstick the register admits — and that statistic was structurally impossible to compute: the groups were built from a list the null copies had already been filtered out of, and the null map was then assembled by scanning those groups for rows with a null tag. There were none, by construction. With every null share null, the comparator's first key returned -1 for EVERY pair, so the chain never reached plateau width, the asset share or money: not a wrong order, not an order at all — a comparator inconsistent with itself. Three configurations with plateau widths 9/5/1 came out 5, 1, 9. Shipped by me the day before, in the change that reordered this list onto register-admitted statistics after the owner challenged the old order. The tests I wrote read the comparator's SOURCE and passed — they saw the right words in the right order | grouping and ranking live in ONE function, rankDeclaredConfigs, which owns both halves: splitting them is exactly how a caller grouping one way and a ranker assuming another survived. The comparator returns 0 when neither side has a measured null so the declared secondary keys apply. The test LIFTS THAT FUNCTION OUT OF THE PAGE BY NAME AND RUNS IT — a copy would agree with itself | general-classifier/public/constructing.js (rankDeclaredConfigs); tests/test-declaredset.js (theMeasuredNullIsActuallyMeasured, nullCopiesNeverEnterTheCrossAssetCount, theOrderFallsThroughToPlateauWidthWhenNoNullExists, theMeasuredNullOutranksPlateauWidthAndMoney) | AUTOMATED — all three faults watched failing 2026-08-17. GENERAL RULE ADDED: a statistic that decides an ORDER must be tested by executing the ordering, never by reading its source. Source-reading is how this shipped |
| 149 | Real money and paper money are kept apart because the totals are computed separately | Only the totals were. lib/live/view.js keeps realized and unrealized separate per book — that is R9/QC-127 — and then returns ONE openPositions array carrying a per-row paper flag. Every consumer that shows a count or a table beside a branch-selected P&L has to filter it, and none did: the Trading tab's setup detail table and its Open-positions tile, its Dashboard cards, and the channel summary in lib/live/routes.js all counted both books. A setup that has legally gone paper -> live holds both at once, so the count disagreed with the money above it and fictional positions were listed under a real-money heading. Separately, the same page hardcoded the F1 pilot's protective stop as null on the Setups table, reporting "no stop" whatever stop was applied, while fetchConfigs already returned the real value and drawSetups threw it away | every consumer filters the merged list to the side it is showing money for, and the F1 row reads the applied stop from the pilot status. RULE: a screen that shows real and paper side by side must filter at EVERY render site, and a risk parameter is never a literal on a screen | general-classifier/public/trading.html; general-classifier/lib/live/routes.js; tests/test-live-view.js (everyConsumerOfTheMergedListFiltersItToOneBook, aSetupHoldingBothBooksReportsOnePositionPerSide); tests/test-uicontracts.js (theF1RowReportsTheStopThatIsActuallyApplied) | AUTOMATED — watched failing 2026-08-17: dropping any one of the three filters, or re-pinning stopPct to null, fails its own check |

## Open gaps — items not yet enforced by anything but discipline

These are listed because a MANUAL item with no owner is a lie.

- ~~**G1 — binomial p-values are still printed.**~~ CLOSED 2026-07-29. The
  column is renamed `naive*` and carries a footnote stating it is not a
  p-value, is not evidence, and that scrambled data scored 0.0047 on it.
  Item 7 is therefore AUTOMATED to the extent that the misleading label is
  gone; quoting it as evidence remains a judgement rule.
- **G2 — restarts are not proven idempotent.** Item 14's fix is a longer
  timeout, which reduces the chance of a truncated run rather than making a
  truncated run harmless.
- **G3 — nothing prevents the consuming poller being used.** Both scripts
  exist and only habit keeps the destructive one out of monitors.
- **G4 / G5 — no mechanism.** These are judgement rules. The only enforcement
  is that they are written here and get re-read.
- **G6 — pilot-alert.sh harness is now committed but PARTIAL.** As of the
  2026-08-11 pre-arm review a DRYRUN fixture harness exists
  (scripts/test-pilot-alert.sh) and covers the incident logic: stale-sync gating
  (QC 101), the cooldown watermark and ORDER_REJECT split and arm-refusal paging
  (QC 102). It does NOT yet exercise the heartbeat replay (QC 88/89) or the
  mirror block (QC 92), so those three remain fixture-verified MANUAL until cases
  are added to the same harness — a bounded, known extension rather than an
  absent harness.
- **G7 — the mail verifier's REMOTE grep is still untested end to end.** QC 103's
  decision logic now has a committed unit harness (scripts/test-mail-verify-parse.sh)
  over the parse+bind step, but the SSH grep that produces its input runs against
  the live /var/log/mail.log and no harness covers that boundary (same class as
  QC 93's note). First live owner send after this change is the real check;
  boundQid is printed on every verdict so a rejection is debuggable on sight.
- **G8 — the executor does not yet ROUTE a per-setup sub-account key (QC 125).**
  mx_executor.py signs every order with the box's single ~/.executor-env key, so
  ALL setups on a box share one isolated-margin sub-account (one borrow + balance
  pool). QC 125's transition gate REFUSES a setup going LIVE without its own
  keyRef, which prevents two live setups from ever coexisting on one account today
  — but the executor must actually sign each setup's orders with that setup's
  sub-account key before a second live setup is safe to run. Until then, exactly
  ONE live rail per box (F1's) is supported, and the gate enforces it. The named
  next step: per-setup key resolution in the executor (load the setup's keyRef
  credentials, sign that setup's orders with them) + a wire test that two setups
  on distinct keyRefs never see each other's borrow.
  UPDATE 2026-08-12 (QC 135): the executor now ENFORCES this rather than trusting
  the classifier gate — S2_LIVE_ROUTING=False makes schema-2 PAPER-ONLY on the F1
  box, so no real schema-2 order can reach F1's wallet regardless of the allowlist.
  The G8 work (per-setup key routing) is now what UNLOCKS real schema-2: flip
  S2_LIVE_ROUTING True ONLY in the same change that lands key routing AND a
  box-wide real-exposure ceiling across both rails (review N1).
- **G9 — lower-severity items from the 2026-08-12 independent review, deferred
  (not blockers; schema-2 is paper-only until G8).** The generalized-rail alerter
  (live-alert.sh) does not yet page a schema-2 ORDER_REJECT (an EXIT reject is
  backstopped only by the slower EXIT_OVERDUE) and does not page an absent/
  unparseable mirror.json (only the exists-but-stale case, R12) — both are
  asymmetries where the F1 alerter (pilot-alert.sh) pages but its sibling does
  not. Also: a successful housekeeping SWEEP ORDER_ACK (no setup_id) still resets
  F1's consecutive-reject streak (money-review N3, pre-existing), the SWEEP order
  carries no client_id so it is invisible to resolve_dangling (N4, self-correcting),
  and batch.js getBatch validates String(id) but reads batchFile(id) (harmless for
  real HTTP string params). NOT_LIVE_EXECUTABLE returns HTTP 500 not the intended
  400 (cosmetic). To fix when the generalized rail actually goes live (needs G8).
- **G10 — 'retired ⇒ flat' is assumed, not enforced (confirming re-review
  2026-08-12).** The updateSetup live-gate excludes `retired` on the premise that a
  retired setup has no open positions, but nothing forces a setup flat before
  retiring: `stopped→retired` / `paper→retired` carry no open-position check, and
  updateSetup has no state guard, so a retired setup's executionTargetRef could be
  re-pointed ungated. NOT reachable today — the per-setup executor that would route
  those orders is unbuilt (G8), and the normal path retires only after `stopped`
  exits complete. Named enforcement when G8 lands: refuse `→retired` while the
  setup has open positions, OR keep retired out of executor selection (as
  catalog.js already keeps non-paper/live out of required-pairs). MANUAL until then.

## How an item gets added

When a faulty assumption is caught — by anyone, including by the owner asking
a question that turns out to have an uncomfortable answer — it is added here
in the same session, with its status honestly marked. An item added as
AUTOMATED must have had its check watched failing first.
