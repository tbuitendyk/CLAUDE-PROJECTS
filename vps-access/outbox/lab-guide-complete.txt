Lab guide finished — it now matches the screen top to bottom

tl;dr Done as ordered before the planted-check button: the Bracket lab
guide (the "lab guide" link beside the tab title) now covers the whole
page in on-screen order. It was written before the data manager
existed, so it opened with the sweep form; now it opens where the page
does. Static deploy, nothing restarted, your sweep untouched.
Hard-refresh the guide page to pick it up.

What the guide now covers, in the order you meet it on screen:

 1. Data on server — the data manager: what the table's "to" column
    really means (latest DATE loaded, day-files counted), what
    Refresh / Global Refresh / Download / Trim / Purge each do, which
    ones DELETE data, and that every write refuses while a job runs.
 2. The sweep form — now including the pieces added since the guide
    was first written: the description line, the permute ticks, the
    replication (declared config) control, the trailing-stop sweep
    and its trail-amb warning, and the null-board count with its
    resolution rule (N boards can never say better than 1 in N+1).
 3. Saved sweeps picker.
 4. The job header — every setting that shapes the result on the
    page, plus the verbatim fold-out.
 5. The survivor board — how to read TEST vs HELD-BACK columns and
    the gate/entry cells.
 6. The two null tools.  7. History Tuning.  8. The microscope.
 9. Compare two runs.  Then the three usage paths compared by what
    each can honestly claim.

Next: the planted-check button build, exactly as you ordered it — top
of the lab above Data on server, current release number, PASS / FAIL /
NOT CHECKED quoting version numbers, and the fabricated pair's history
regenerated by the data machinery so it always spans oldest-to-newest
of the real data. That one changes the engine, so its deploy waits for
your running sweep to finish; I'll build and test it fully in the
meantime.

c.
